## Skill
把某类任务沉淀成一个可复用能力包，里面可以包含固定指令、范例、脚本、资源甚至代码。这样模型遇到相似任务时，不是每次都临时写 prompt，而是直接调用这套能力。
## MCP
一个协议层的东西，不是单纯提示词。它解决的是“模型怎么安全、标准化地访问外部工具、数据库、文件、搜索服务”这个问题。
### MCP和Skill一句话区分
- Skill 更像能力模板 / 工作流封装
- MCP 更像外部能力接入协议
## RAG

### 核心流程的三个阶段 (Workflow)

### 1. 检索 R（核心步骤）

- **文档分片 Chunking**
    
    1. 固定长度切分
    2. 按自然结构切分（段落 / 标题 / 句号）
    3. 滑动窗口（带重叠，防止语义切断）
    
- **向量化 Embedding**
    
    用向量模型把文本块转为一维向量
    
    池化方式：取 CLS 向量 / 平均池化
- **相似度计算**
    
    点积、欧式距离、余弦相似度（取 Top-K）
- **高效检索**
    
    ANN 近似最近邻（聚类粪桶或图搜索）（粗召回）→ Rerank 重排（精排）
- **向量数据库**
    
    存向量、建索引、快速高维相似检索

### 2. 增强 A

把**检索到的 chunk + 用户问题**拼成新提示词，让模型 “带资料答题”。

### 3. 生成 G

大模型基于增强提示词，逐词预测输出答案。

#### **A. 索引阶段 (Indexing) —— 数据结构化**

这是预处理过程，目的是将非结构化文档转变为计算机可理解、可对比的数学表达。

1. **清洗与分块 (Chunking)**：将长文档切分为固定长度或基于语义的段落（Chunks）。
    
    1. _逻辑前提_：LLM 的上下文窗口有限，且检索过长的文本会导致噪声干扰。
        
2. **向量化 (Embedding)**：利用 Embedding 模型将文本块转换为高维向量（Vector）。
    
3. **存储**：存入向量数据库（Vector DB，如 Pinecone, Milvus, Weaviate）。
    

#### **B. 检索阶段 (Retrieval) —— 寻找相关性**

当用户提出问题（Query）时：

1. **查询向量化**：将用户的问题转换为同空间的向量。
    
2. **相似度计算**：计算问题向量与数据库中各文本块向量的余弦相似度（Cosine Similarity）。
    
3. **Top-K 召回**：取出得分最高的 K个文本块。
    

#### **C. 生成阶段 (Generation) —— 注入上下文**

1. **Prompt 注入**：将检索到的文本块作为“背景事实”填入预设的模板中。
    
    1. _模版示例_：`已知信息：{检索内容}。请根据已知信息回答问题：{用户提问}。如果信息不足，请回答不知道。`
        
2. **模型推理**：LLM 此时不再仅靠权重记忆回答，而是基于提供的证据进行归纳总结。
## Transformer
![[Pasted image 20260327214932.png]]
## 平常怎么写prompt
https://asterzephyr.github.io/posts/-prompt/

**Prompt 的核心组成部分**
1. 任务
2. 实例
3. 输出格式
4. 角色
5. 任务要求/步骤
6. 额外信息
7. 内容格式
