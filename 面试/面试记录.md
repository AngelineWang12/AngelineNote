# 自我介绍

面试官好，我是王仪霖，目前是电子科技大学的硕士研究生，研究方向是差分隐私，具备一些后端开发的经验。最近的一段经历是在美的物流部门实习，负责制造供应链平台中**库存转移**和**库存属性转移**两块核心业务。在这个过程中，采用策略模式处理四种不同的转移类型，用**策略模式**实现可扩展的转移流程，保证新增类型时只需实现策略类并注册即可。 在高并发拼团场景下，设计了基于 **Redis 的无锁抢占**，显著减轻了数据库行锁竞争并提高了系统吞吐。 在技术能力上，我熟悉JUC并发编程、MySQL、Redis ，拥有良好的问题驱动和学习能力。

难点

- S（情景）：美的供应链平台是自研的，服务于内部工厂和海信等外部客户，管理从订单、排产、仓储到物流的全链路信息。我所在的物流部门主要负责仓储和物流模块。
    
- T（任务）：我接到的核心任务是开发“库存属性转移”功能。一句话说清业务本质：在不移动实物的情况下，改变库存物料的归属订单（例如，从A订单划拨10个物料到B订单）。
    
- A（行动）：
    
    - 技术挑战：库存绑定的“批次号”同时承担了唯一标识和出库顺序两个职责。在属性转移时，新生成的批次号必须保证全局唯一，且顺序与原批次接近。
        
    - 初步方案：从数据库查询历史最大序列号+1。立即点出缺陷：这会导致与当日实时入库的序列号产生冲突。
        
    - 深入思考：我考虑了两种方案。方案一：判断日期，如果是当天就走Redis，否则走数据库。方案二（最终采用）：将批次号扩展两位，普通入库为00，属性转移则递增最后两位。
        
    - 架构思考：事后复盘，面试官指出更好的设计是将“唯一性”和“顺序性”解耦，用两个字段来维护。我深刻认同，这是更优雅的解决方案。
        
    - 其他亮点：由于转移类型多样（订单转订单等），我采用了策略模式来消除大量的`if-else`；为了保证多表操作的一致性，使用了Spring事务。
        
- R（结果）：功能按时上线，支撑了业务的灵活调拨。通过这次实践，我不仅完成了需求，更对高内聚低耦合的设计原则和分布式ID生成方案有了更深的理解。
    

面试官您好，我是王仪霖，本科毕业于杭州电子科技大学信息安全专业，目前是电子科技大学的硕士研究生。我的研究方向主要是差分隐私，同时在后端开发方面积累了一些经验。

在美的集团实习期间，我参与制造供应链平台的开发：负责设备停线、出勤等核心表的批量 CRUD 与导入校验，用 Redis 缓存校验结果；在库存转移中落地 Redisson 分布式锁保证并发安全，在库存属性转移中用策略模式配合事务保障多表一致。

同时我独立实现过高并发拼团平台：通过规则树+策略模式支持满减/折扣/N 元购多种营销优惠策略；设计Redis无锁化拼团库存抢占和恢复库存处理，减轻数据库行锁独占的压力。

在技术能力方面，我熟练掌握Java基础，JUC并发编程、MySQL和Redis等技术。此外，我曾获得**全国大学生数学建模竞赛浙江省一等奖**以及**全国研究生网络安全创新大赛二等奖**，具备较强的算法和工程实践能力。

以上是我的自我介绍。期待与您深入交流，谢谢！

  

面试官您好，我是王仪霖，本科毕业于杭州电子科技大学信息安全专业，目前是电子科技大学的硕士研究生。我的研究方向主要是差分隐私，同时在后端开发方面积累了一些经验。

在项目经历方面，我主导并开发了**基于Netty的即时通讯系统**，运用Netty、Redis、 RabbitMQ等技术，优化了单聊消息的可靠性并实现聊天记录异步持久化，此外为了解决通信过程的粘包问题，设计并实现了私有协议。我还实现了**手写RPC框架**，基于Netty和ZooKeeper，支持服务注册发现、负载均衡，并实现了服务限流和熔断机制。

在技术能力方面，我熟练掌握Java基础，JUC并发编程，熟悉Spring、MySQL和Redis等技术。此外，我曾获得**全国大学生数学建模竞赛浙江省一等奖**以及**全国研究生网络安全创新大赛二等奖**，具备较强的算法和工程实践能力。

以上是我的自我介绍。期待与您深入交流，谢谢！

  

**安全**

面试官您好，我是王仪霖，本科毕业于杭州电子科技大学信息安全专业，目前是电子科技大学的硕士研究生。我的研究方向主要是差分隐私。

在研究生期间，我提出了一种适用于集值数据联合分布分析的差分隐私协议——SVJDA。该方案通过将用户数据视为稀疏向量，有效降低了噪声并提高了隐私保护能力，理论和实验表明它能在保持隐私的同时，显著提升联合分布估计的准确性。我还设计了一种面向图数据的具有鲁棒性的差分隐私协议，利用图数据中的冗余信息能够有效检测恶意用户投毒攻击，提供了一种新的框架来评估协议的稳健性与抗攻击能力。

在技术能力方面，我熟练掌握Java基础，熟悉MySQL和Redis。此外，我曾获得**全国大学生数学建模竞赛浙江省一等奖**以及**“中国网谷·华为杯”全国研究生网络安全创新大赛二等奖**，具备较强的算法和工程实践能力。

以上是我的自我介绍。期待与您深入交流，谢谢！

# ai项目

ai可编排服务系统是一个什么项目

15、谈一谈对ai的看法

16、mcp和function call区别

17、知识文档智能解析是用什么技术进行解析的

18、文档内容怎么提取出来的

19、流式对话接口是用什么技术提供的？接口是一个什么协议什么样的网络通信

20、sse是双向通信还是单向通信的

# 2.28 腾讯TEG云架构开发

1. 自我介绍
    
2. 项目：自定义消息体提升系统拓展性 私有协议设计？
    
3. TCP和UDP的区别
    
4. TCP和UDP的效率差异体现在哪里
    
5. 四次挥手的过程
    
6. 多端登录，心跳机制、消息收发、服务注册 介绍其中的两个业务
    
7. 项目实现至少需要多少台设备
    
8. 收发消息真的实现了吗？能收到吗
    
9. 故障降级和令牌桶算法限流 如何实现的
    
10. 手撕LRU
    

# 3.15 美团AI面

1. http的请求头和响应头
    
2. 如何利用find查找文件
    
3. 系统日志怎么查看，有哪些
    
4. sql注入，如何解决
    
5. linux权限，如何修改文件和目录的权限
    
6. Object 类的常见方法有哪些？
    
7. 封装的作用
    
8. 设计评论系统的数据结构
    

# 3.20 美团一面

1. spring和springboot的区别
    
2. mysql隔离机制
    
3. redis持久化
    
4. mysql和redis的使用场景
    
5. 手撕：[986. 区间列表的交集 - 力扣（LeetCode）](https://leetcode.cn/problems/interval-list-intersections/)
    

# 3.26美的

1. Redis数据结构
    
2. 项目中用到了什么数据结构
    
3. Redis实现消息队列
    
4. 用过的数据库
    
5. 数据库的隔离机制
    
6. 除了串行化，避免幻读的方法
    
7. 锁的类型
    
8. 慢查询优化
    
9. 分布式事务
    
10. Zookeeper服务发现
    
11. 高可用性是怎么实现的
    
12. zookeeper数据结构、临时节点的特性
    
13. 除了zookeeper还有什么别的服务注册中心
    
14. Spring cloud实现负载均衡
    

# 4.3 美团数开

1. 为什么本科专业是信息安全？调剂的吗
    
2. 介绍差分隐私
    
3. osi7层模型
    
4. 四层模型 为什么要从7层到4层
    
5. ipv4和ipv6
    
6. url的访问过程 能不能用url代替IP地址
    
7. tcp三次握手，能不能是两次，为什么第二次除了ack还要发syn
    
8. http和https的区别，https的加密方法
    
9. rsa加密的原理
    
10. tcp和udpd的区别
    
11. 做题：三数之和 时间复杂度 空间复杂度
    
12. 之前做过的项目遇到的最大的困难
    
13. 差分隐私的最终输出是什么
    
14. RabbitMQ的作用是什么 三个 可能存在什么问题
    
15. 如何实现分布式消息的有序性
    
16. 事务的隔离机制
    
17. 第二个项目的难点
    
18. B树和B+树的区别 效率具体体现在哪
    
19. 为什么要引入红黑树和普通的排序二叉树的区别
    
20. 做题：二叉树层序遍历
    

# 4.3 美的

1. 自我介绍
    
2. ai对于开发的影响
    
3. 项目：异步持久化怎么实现的
    
4. 项目：多端登录消息拉取 数据怎么存的 具体结构
    
5. redis的部署模式，哨兵模式的缺点
    
6. 查看进程、线程占用情况
    
7. 左连接右连接的区别
    

# 4.7 饿了么

1. 自我介绍
    
2. 介绍项目难点 怎么实现的
    
3. 多端登陆实现 怎么下线的 下线通知怎么发的
    
4. 怎么理解redis
    
5. 怎么理解mysql、b+树
    
6. 怎么理解rabbitmq，怎么实现消息的生产和消费
    
7. Sychronize reentrantlock 底层
    
8. 怎么理解threadlocal 什么场景下使用
    
9. 服务发现怎么实现的，为什么要用zookeeper，负载均衡怎么实现的
    

ZooKeeper 的作用就是提供无单点问题的分布式协调服务。

10. 有成就感的事情
    
11. 差分隐私
    

# 4.18 饿了么

1. 自我介绍
    
2. 介绍差分隐私
    
3. 项目：RPC
    
4. 为什么要写RPC、讲一下RPC的流程
    
5. Zookeeper 的数据结构以及如何避免脑裂（选举机制）
    
6. 限流和熔断是如何实现的、限流设置服务端还是用户端、为什么
    
7. 如果用户请求的参数有问题会导致熔断吗（例如请求某个id的数据但是id不存在如何解决）
    

**返回错误码**：对于请求某个 `id` 不存在的情况，可以返回 `404 Not Found` 或 `400 Bad Request` 等错误码，而不是让服务抛出异常，这样就不会影响熔断器的错误统计。

8. 限流：如果0秒到达了多个请求，将令牌用完了，拒绝之后的请求吗（如何解决）
    

##### 3.1 **排队等候**（Queuing）

在令牌用尽时，可以选择将请求排队等待。新的请求将在令牌生成时逐步获得令牌，按顺序处理。

- **实现方式**：将请求放入一个等待队列中，并按照令牌桶的生成速率逐个处理这些请求。
    
- 例如：当请求 1 进入时，它获得令牌，执行请求；请求 2 和请求 3 被排队等待，直到令牌被释放并分配给它们。
    
- **优点**：可以平滑突发流量，避免丢弃请求，保持系统的连续性。
    
- **缺点**：会增加响应时间，特别是在流量突然增加的情况下，可能会导致请求处理的延迟。
    

##### 3.2 **请求重试（Retry）**

对于被拒绝的请求，可以设置重试机制。请求失败后，可以在一段时间后重试获取令牌，直到请求成功为止。

- **实现方式**：请求被拒绝时，客户端或服务端会记录请求失败，并在一定的时间间隔后尝试重试请求，直到令牌桶有令牌可以消费为止。
    
- **优点**：请求不会完全丢失，保证了系统的稳定性。
    
- **缺点**：可能会增加系统的负载，特别是当请求失败并且重试次数过多时。
    

##### 3.3 **限流降级（Backpressure）**

当流量激增时，可以通过 **降级策略** 来减少部分请求的处理压力，确保系统可以承受过高的负载。降级的方式包括：

- **优先级控制**：将请求按优先级分配令牌，高优先级的请求优先处理，低优先级的请求被延迟处理或丢弃。
    
- **请求拒绝**：对于低优先级或不重要的请求，直接丢弃或返回友好的提示，减少系统压力。
    

**实现方式**：设置规则限制一些非核心业务（例如日志记录、统计分析等低优先级请求），或使用缓存和队列控制高频请求的流量。

##### 3.4 **动态令牌生成速率**

可以根据流量情况动态调整令牌生成速率。例如，在系统负载较低时，可以增加令牌的生成速率；而在负载过高时，降低令牌生成速率，平滑流量。

**实现方式**：监控系统负载、请求数量和队列长度等指标，动态调整令牌桶生成令牌的速度。

9. 一致性哈希算法和别的哈希的区别
    

普通哈希算法：对服务器的数量进行取模，（存在问题：服务器数量变动的时候，所有缓存的位置都要发生改变

一致性哈希：对2^32取模，将整个哈希值空间组织成一个虚拟的圆环，尽可能减少了服务器数量变化所导致的缓存迁移。

一致性Hash算法在服务节点太少时，容易因为节点分布不均匀而造成数据倾斜（被缓存的对象大部分集中缓存在某一台服务器上）问题，一致性Hash算法引入了**虚拟节点**机制，即对每一个服务节点计算多个哈希，每个计算结果位置都放置一个此服务节点，解决数据倾斜问题。

10. 除了对于java的收获有没有别的收获
    
11. JUC包用过什么
    
12. Sychronize底层、改进变化、线程池的参数、初始创建有几个线程（1个）
    

moniter监视器

Sychronize锁升级：

锁主要存在四种状态，依次是：无锁状态、偏向锁状态、轻量级锁状态、重量级锁状态，他们会随着竞争的激烈而逐渐升级。注意锁可以升级不可降级，这种策略是为了提高获得锁和释放锁的效率。

> ps:有的观点认为 Java 不会进行锁降级。实际上，锁降级确实是会发生的，当 JVM 进入安全点（[SafePoint](https://link.zhihu.com/?target=http%3A//blog.ragozin.info/2012/10/safepoints-in-hotspot-jvm.html)）的时候，会检查是否有闲置的 Monitor，然后试图进行降级。

初始创建有1个线程，直至核心线程数，才进入等待队列

13. JVM结构、堆和栈存在的问题、什么时候需要检查堆和栈、如何避免OOM和栈溢出
    

堆、方法区、虚拟机栈、本地方法栈、程序计数器

**栈可能会出现两种错误：**

- **`StackOverFlowError`****：** 如果栈的内存大小不允许动态扩展，那么当线程请求栈的深度超过当前 Java 虚拟机栈的最大深度的时候，就抛出 `StackOverFlowError` 错误。
    
- **`OutOfMemoryError`****：** 如果栈的内存大小可以动态扩展， 那么当虚拟机在动态扩展栈时无法申请到足够的内存空间，则抛出`OutOfMemoryError`异常。
    

**堆**最容易出现的就是 `OutOfMemoryError` 错误，并且出现这种错误之后的表现形式还会有几种，比如：

- **`java.lang.OutOfMemoryError: GC Overhead Limit Exceeded`**：当 JVM 花太多时间执行垃圾回收并且只能回收很少的堆空间时，就会发生此错误。
    
- **`java.lang.OutOfMemoryError: Java heap space`** :假如在创建新的对象时, 堆内存中的空间不足以存放新创建的对象, 就会引发此错误。(和配置的最大堆内存有关，且受制于物理内存大小。最大堆内存可通过`-Xmx`参数配置，若没有特别配置，将会使用默认值，详见：[Default Java 8 max heap size](https://stackoverflow.com/questions/28272923/default-xmxsize-in-java-8-max-heap-size))
    

**如何避免OOM和栈溢出：**

1、尽早释放无用对象的引用

2、使用字符串处理，避免使用String，应大量使用StringBuffer，每一个String对象都得独立占用内存一块区域

3、尽量少用静态变量，因为静态变量存放在永久代（方法区），永久代基本不参与垃圾回收

4、避免在循环中创建对象

5、开启大型文件或从数据库一次拿了太多的数据很容易造成内存溢出，所以在这些地方要大概计算一下数据量的最大值是多少，并且设定所需最小及最大的内存空间值。（在数据量比较大的时候应该分批处理数据避免OOM）

栈溢出：检查递归错误

14. GC算法、CMS的流程
    

# 5.7 客路

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=MWFkNzRjNDU4YjcxYmI0MTdlOTBlZmU4YTc2ZDA2ODZfWXZoMkhNd29Fb21uUm1UV3hBdENQanhFVjRaUUVPNGJfVG9rZW46THI5YWJkdENPb2tHdHN4WHNKTGNtVnR4bmRoXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

1. HashMap底层数据结构
    
2. HashMap的扩容机制
    
3. Java如何实现线程池
    
4. 进程线程的区别
    
5. 进程间通信
    
6. redis的set和zset的区别
    
7. zset底层数据结构（压缩链表和跳表）
    
8. 虚拟内存（1G内存能否打开2G文件，虚拟内存通过页表映射地址。）
    
9. InnoDB和MyISAM底层数据结构
    
10. 数据库死锁的原理 条件
    
11. 即时通讯系统的挑战
    
12. 多端消息同步、离线消息拉取、DFA检测
    
13. RPC和HTTP的区别
    
14. 令牌桶限流、熔断
    
15. 如果给你一个项目，你会怎么入手，你在什么时候会向导师求助
    

# lbq面经：

## 京东3.22

1. 介绍一下自己
    
2. 你们专业（网络工程）和计算机科学与技术有什么区别？
    
3. 操作系统，机组这些课程你是通过什么途径学习的？
    
4. 网络空间安全领域你的具体研究方向是什么？
    
5. 你们实验室现在做的什么项目？
    
6. 这个项目的一个背景是什么样子？他一个整体的一个架构和考虑是什么样子？然后你在里面主要负责哪些事情？
    
7. 这个平台，现在有在用吗？这是一个什么样的一个多大的规模在用？你们这个服务是怎么部署的呢？
    
8. 我看你也提到了。你会用到像redis 中间件kafka以及数据库的东西，对吧？消息队列这一块像我看你也提到了，rocketmq和kafka。你知道他一个内在的设计是什么样子的吗？比如他是怎么做到高性能支持高并发高可用的吗？
    
9. Tcp 和udp协议的区别，你能说一下吗？
    
10. 拥塞控制机制你能详细说一下吗？
    
11. Socket编程详细讲一下
    
12. 你了解哪些语言？都用来写过写什么？
    
13. Mysql的索引的最佳实践说一下。
    
14. Linux系统你了解吗？如果比较卡你会如何排查？
    
15. 讲一下你对虚拟内存的理解。
    
16. 平常就是在提升自己的技术方面，你会你平常有看一些，你是什么通过什么途径来，比如说来提升自己的技术能力的呢？有看过一些开源项目吗？
    
17. 对于这个云计算这一块有过什么接触或者什么了解吗？
    
18. 容器k8s,就是这些东西有使用过，或者是有了解过这方面的东西吗？
    

## 4_9 百度 研发工程师 tob产品研发 一面 过

1. 自我介绍
    
2. 这个视频平台还挺大的，是你一个人做的吗？
    
3. 第一个项目和第二个项目你能大概介绍一下它的背景和你的工作吗？因为我这边光看到你写的技术栈。然后他这个项目的需求什么东西呢？
    
4. 今天是一面项目，大概就这样，我们重点考察还是以基础知识为主，先稍微介绍一下osi的七层系统，简单介绍一下osi
    
5. 你当时做开发的时候是不是大概就是到tcp这个层级为止，往下就没再怎么关注过了，对吧？还往下有关注吗？四层以下？
    
6. 介绍一下tcp udp协议的区别，quic协议，听过吗？介绍一下TCP超时重传和拥塞控制机制。发生拥塞之后窗口如何减小？减小到多少？就或者说我收到了三个重复的ack 以后，这个窗口会怎么样？
    
7. SYN洪流攻击讲一下
    
8. 我刚刚看你在这个项目里边写到你通过多个存储账号凭证反复的突破了他qps的限制，对吧？那对，那如果让你来开发的话。你会想到用怎样的方法去避免有人 那么对你的系统做这种突破限制？
    
9. 你们的这个后端是一个分布式的。还是一个单机的？你有想过用zookeeper和你的第一个弄在一起，搞个分布式这样做吗？
    
10. 浏览器输入URL后发生的全流程
    
11. dns两种两种查询方式，知道吗？（反向解析是另外一种）
    
12. 说说一致性哈希，你还记得一致性哈希代码怎么写的吗？
    
13. 用户态和内核态的区别，讲一下零拷贝，线程进程，进程通信方式
    
14. 进程之间怎么保护它的内存不被其他进程所占用？
    
15. 你在开发项目的时候有用过多线程吗？不是线程池。
    
16. 线程安全的实现方法有哪些？死锁的概念和解决方法。
    
17. 浅拷贝和深拷贝区别，置换算法的lru和lfu知道吗？这两个置换算法的缺点是什么？（需要点明具体这两个算法不适用的场景）
    
18. http1,http1.1,http2
    
19. 令牌桶听过吗？
    
20. 说一下redis的核心特性（高性能、高可用）
    
21. MYSQL的隔离级别，开发的时候mysql的那些地方可能会出现性能瓶颈？如何解决？
    
22. Sql注入说一下
    
23. 打家劫舍代码题
    

## 4_14 百度 研发工程师 tob产品研发二面 挂

1. 自我介绍
    
2. 忘记录屏了，主要围绕项目内容提问以及rocketmq、zk、es比较底层的机制，这几个组件不太熟悉，只回答了基本的，后续反问，面试官回应：这边语言不固定，需要深入了解rocketmq、zk、es等
    

## 3_31 美团 软件开发 一面 挂

3. 自我介绍
    
4. DFS,BFS,匈牙利算法的过程以及应用场景
    
5. 讲一下ZK原理吧
    
6. 代码题目：两个岛屿的最近距离（找出两个岛屿之间经过0最少的一条路径）
    
7. 下面这个语句有什么问题：
    

ALTER TABLE orders ADD INDEX idx_createtime_status(create_time,status);

SELECT * FROM orders WHERE DATE_FORMAT(create_time,'%Y-%m')='2023-05' AND status = 1 ORDER BY amount DESC LIMIT 10。答create_time用了DATE_FORMAT函数，不能走索引，然后让我改，我当时脑袋没转过来，我说是用int型的unix时间判等，说这样改了表结构不太好，后续反问的时候说是用between就行

8. 看一段代码，主要是出现了线程不安全的情况，解决：加锁/数据库记录新增一个字段实现乐观锁
    
9. Redisson实现分布式锁的原理是什么？（lua脚本）
    
10. 讲一下rocketmq的原理，比如说他是怎么去流量萧峰的，然后他怎么做延时队列
    
11. 比如说一个topic,它在多个block上面都有，然后又有，比如说有n个broker,m个consumer,他们是怎么应对他们的？什么对应关系是怎么怎么确定的？（consumequeue和commitlog是一起发送的）
    

## 4_15 淘天 商品与库存 一面 当晚约的16号二面

1. 自我介绍+项目介绍
    
2. 你觉得在这个项目里最大的难点是什么？你做合并请求最初的出发点是什么？目的是什么？
    
3. 布隆过滤器的原理是什么？
    
4. jwt加redis存储，这样的校验方式，它的优势是什么？(方便幂等性处理，还可以手动拉黑)
    
5. 为什么不适用cookie-session？（需要分布式存储session，并且有安全问题CSRF一类）
    
6. 你怎么知道你被黑客攻击了？（行为分析+设备指纹）
    
7. ES分词器原理是什么？
    
8. Hashmap原理，反射用来解决什么问题？反射的话，一般都是会有性能问题的。我们在实践上怎么去优化反射的性能呢？
    
9. 双亲委派，AOP如何理解？
    
10. Mysql一致性如何实现的？（AID实现C，得讲AID如何实现的）说说事务。
    
11. Redis高性能原理（内存、0拷贝、reacot模型、数据结构优秀）
    
12. 依赖注入的最大价值是什么？（解耦、简化开发）
    
13. Bean的循环依赖在代码层面上如何解决？
    
14. Mysql慢查询
    
15. 反问，回答：比较优秀
    

## 4_16 淘天 商品与库存 二面 18号挂了

1. 研究方向，聊了论文
    
2. 讲一下你的学习路线，有看过中间件的源码吗？你是没有实习经历对吧？
    
3. Tcp/udp，tcp有几个状态，对应什么情况。http协议以及发展过程讲一下
    
4. JDK,JRE,JVM关联，反射是什么？
    
5. 我看你之前好像有过好几次的面试记录，是当时是面的不合适吗？还是怎么样？我看你面了好几次。
    
6. Java当中线程的创建方式它有哪几种？（继承、实现几种接口放入构造器，用已有的类completablefuture）runnable和callable的区别？java进程启动后是如何分配内存空间的？创建线程池的方法有哪些？（自定义参数，四种线程池）
    
7. 什么是IOC，AOP
    
8. 写一个双向链表类，头部/尾部的增删
    
9. 跟我讲一讲你最了解的某一个那个java的技术框架。（锁，AQS，retreentlock，synchornize底层以及synchornize锁升级机制）
    
10. Nacos底层原理（不太懂）
    
11. Mysql的索引、事务（日志）
    
12. 你现在面了哪些公司？手里有哪些offer?(推进比较慢，还没有)
    
13. 是问一下你之前有没有实习经历，没有实习经历。不决定，能不决定你这个面试好还是不好，跟没有关系。
    
14. **挂了**
    

## 4_14 腾讯云智 运营开发 一面 16号约二面

1. 八股盛宴，忘记录制了
    
2. 面呗有问题，16号来写到题目
    
3. 反问哪里需要提升：一些底层原理，主要是netty.，zk
    
4. 你起码可以挑一个开源组件去系统的深入的去了解一下，因为当然很因为要是那个如果是相对简单的开发的话，你可以说老是那种只调接口嘛。而是但是，而是还有很多问题的话，是需要老师那个结合，第一就是你，你的技术视野进入世界的话，一定是要深入了解团去做。第二，在处理实际问题的时候，你不可能别人 你理所当然的就认为是别人提供的软件都都是可靠的。那如果你如果不去深深了解的话，是那个就是你怎么来处理相关问题，那个。第三是这些优秀的课程软件本身是去学习的话，对自己就是一个是那个技术设计的提升。
    
5. 问我为什么没有投TX本部（面评烂了）
    
6. 其实这些所这些的面试题都是结合我刚刚结合我刚刚说的话。是那个你对其实你用的很多东西也要是那个就是就是在我看来比较似是而非，其实你的所有评价跟我觉得都是在这里，你就如不管是后面实习还是准备的话，是其实深入需要深入的去那个看一下对行。
    
7. 16号：二叉树深度（18min提醒我是不是存在边界问题，要不要再看一下，我说再看一下，过了2min，问我你确定是这样吗？我说没问题就是这样，不需要改）
    
8. 写完30min后约2面
    

## 4_18 腾讯云智 运营开发 二面 约20号HR面试

1. 自我介绍
    
2. AOP和IOC的理解（简化开发à注解à动态代理à反射）
    
3. 除了他自身的代理之外。像cglib有了解吗？比如它底层如何实现的这种，比如说因为java的话，jdk自带的话，它是你需要实现一个interface嘛是吧？(spring注解底层都是cglib，基于继承可以直接调用子类，效率高于jdk接口动态代理)
    
4. 再下面有了解吗？比如说他。他怎么继承的时候？比如说那个ASM库有了解过吗？（不知道ASM）
    
5. 声明式事务，这种事务的传播方式底层如何实现的？
    
6. Threallocal的理解（ThreadLocalMap）
    
7. Volatile底层原理（读写屏障，汇编层面添加了lock语句防止指令重排序）
    
8. Gcroots的作用（垃圾判定、回收算法、CMS的三色标记）
    
9. Concurrenthashmap（…）
    
10. Java程序跑得慢如何排查（扯得是内存方面的排查，堆、栈、直接内存）
    
11. Es，zk的原理（倒排索引、分片与刷新，zk的数据结构、通过节点id以及事务id选主实现崩溃恢复，后续主从同步）
    
12. IO多路复用（NIO的一种实现，讲了下selector,channel,buffer）
    
13. Redis源码看过吗？高性能高可用原理
    
14. Epoll与select/poll的区别，epoll的两种触发方式
    
15. Closed_wait是什么时候发生的
    
16. 将一个最了解的数据结构（B+树）
    
17. 反问：几轮面试？回答：2轮技术面1轮HR面试，是大数据开发中心和hadoop spark有关，做bug fix和业务关系比较小
    

## 4_18 腾讯WXG 后台开发 一面 进入复试状态还没约

1. 三道题：合并区间，IP转数值，linux命令（查找名字里包含tencent并且过滤包含tecent_wxg的命令）
    
2. 鉴权是怎么做的？讲讲流程（终于，终于就这样直接地问出来了啊！）
    
3. 怎么保证这个token是不会被挟持的？（瞎说了一下加SameSite，这不是cookie吗？害真能说啊bin）
    
4. 首先判断他是不是被劫持。判断是不是被劫持，就是用设备指能设备指纹或者行为分析一下他的用户行为，如果已经确定他是被劫持，那就去拉黑，那就改成单token+redis便于拉黑
    
5. https讲了一下，然后问我能不能在应用层也做一个ssl握手一样的非对称加密，我就回到之前的问题说token在header里，用的https的话，jwt应该会被加密怎么劫持呢？说明ssl被破解了，那么应用层再加一次类似于ssl的话是来解决这个问题吗？面试官说是其实是可以反解出来的。他们是已经有做过了。
    
6. 视频上传如何保证视频有序？（请求中有分片号，后直接调用的minio接口实现的）minio的原理你知道吗？不晓得
    
7. 通过什么判断需要重传？（对啊，问这个嘛）
    
8. 讲一下ES mysql的区别，es里它的查询是不是实时的？就比如说我写入了一条数据，马上就能查到，这是怎么实现的？
    
9. rocketmq 怎么保证可靠性消费的？(持久化、消息确认)，broker选主是怎么做的？
    
10. 讲一下数据同步的流程（对的对的）
    
11. Mysql->mq->redis->es中间有一步断掉了怎么办？（对的，问这个）
    
12. Es除了搜索还用在了哪里？有对视频内容做解析吗？还是说只做了标题、关键字之类的（是的）
    
13. 私聊是怎么做的？websocket连接怎么建立的？（http upgrade）连接断了怎么办？
    
14. 这里你还说了，你做了一个请求的合并，你为什么要在高于预值时合并请求？在他之前不可以这么做吗？（讲了流程，说明一下低于阈值合并不一定会更快）
    
15. Mysql索引，隔离级别…
    
16. Redis持久化，线程模型，雪崩、击穿、穿透
    
17. 一致性哈希
    
18. 操作系统的启动过程（不会）
    
19. 虚拟内存（答得分页分段，但他想问的是堆栈之类的）
    
20. 进程通信，哪个最快？
    
21. 四次挥手，timewait
    
22. Io多路复用，epoll触发方式
    
23. 反问哪里需要提升：提升的话，感觉可以去把那些项目简历讲的还是可以的，你表达也比较清晰，真是挺不错的，对用的知识也能有去了，有去进行一个原理，进行一个了解，还要提升的话。可能你在下面那些基础知识像一些进程线程或者操作系统或者是一些网络，还有一些io多路复用，这些东西可以再去了解一下这些基础知识也是挺重要的，对，这个是支撑你做那些项目的一个最底层的技术。
    

# 美团面经

## 美团数据开发一面

美团数开一面

◦自我介绍

◦问了一下实习觉得遇到困难的事

1. 用大模型多吗什么场景用 prompt方法
    

写提示词的一些原则

- 清晰的指令：足够清晰明确地说明你希望模型为你返回什么，最后更加细致地说明需求，避免模糊表达。
    
- 提供上下文和例子：给出较为充分的上下文信息，让模型更好地理解相关背景。如果能够提供示例，模型能表现更好（类似传统LLM中的in-context learning）。
    
- 善用符号和语法：使用清晰的标点符号，标题，标记有助于转达意图，并使输出更加容易被解析
    
- 让模型一步一步地思考：在这种方法中，模型逐步进行思考，并呈现出涉及的步骤，这样做可以降低结果的不准确的可能性，并对模型响应的可解释性有很大的帮助。
    
- 激励模型反思和给出思路：可以在prompt中用一些措辞激励模型给出理由，这样有助于我们更好地分析模型生成结果，同时，思维过程的生成，也有助于其生成更高质量的结果。
    
- 给容错空间：如模型无法完成指定的任务，给模型提供一个备用路径，比如针对文本提问，可以加入如果答案不存在，则回复“无答案”
    
- 让模型给出信息来源：在模型结合搜索或者外部知识库时，要求模型提供他的答案的信息来源，可以帮助LLM的答案减少捏造，并获取到最新的信息。
    

  

2. final ？可以修饰类吗
    

final修饰符用于禁止类被继承或方法被重写，抽象类不能加final修饰

3. ◦sql having知道吗
    

在 SQL 中增加 HAVING 子句原因是，WHERE 关键字无法与聚合函数一起使用。

HAVING 子句可以让我们筛选分组后的各组数据。

4. linux查看进程命令 新建文件 查看文件
    

**查看进程：**可以通过 ps 命令或者 top 命令来查看进程的状态。

**新建文件：**

`mkdir [选项] DirName`

通过 `mkdir` 命令可以实现在指定位置创建以 DirName(指定的文件名)命名的文件夹或目录。要创建文件夹或目录的用户必须对所创建的文件夹的父文件夹具有写权限(了解Linux文件-文件夹权限请点击这里)。并且，所创建的文件夹(目录)不能与其父目录(即父文件夹)中的文件名重名，即同一个目录下不能有同名的(区分大小写)。

**查看文件：**

`cat [选项] [文件...]`

查看目录文件 ls

5. spark工作流程
    

  

6. ◦进程和线程区别
    

一个进程中可以有多个线程，多个线程共享进程的**堆**和**方法区 (JDK1.8 之后的元空间)资源，但是每个线程有自己的程序计数器**、**虚拟机栈** 和 **本地方法栈**。

**总结：**

- 线程是进程划分成的更小的运行单位,一个进程在其执行的过程中可以产生多个线程。
    
- 线程和进程最大的不同在于基本上各进程是独立的，而各线程则不一定，因为同一进程中的线程极有可能会相互影响。
    
- 线程执行开销小，但不利于资源的管理和保护；而进程正相反。
    

7. 进程之间通信方式
    

**管道/匿名管道(Pipes)** ：用于具有亲缘关系的父子进程间或者兄弟进程之间的通信。

**有名管道(Named Pipes)** : 匿名管道由于没有名字，只能用于亲缘关系的进程间通信。为了克服这个缺点，提出了有名管道。有名管道严格遵循 **先进先出(First In First Out)** 。有名管道以磁盘文件的方式存在，可以实现本机任意两个进程通信。

**信号(Signal)** ：信号是一种比较复杂的通信方式，用于通知接收进程某个事件已经发生；

**消息队列(Message Queuing)** ：消息队列是消息的链表,具有特定的格式,存放在内存中并由消息队列标识符标识。管道和消息队列的通信数据都是先进先出的原则。与管道（无名管道：只存在于内存中的文件；命名管道：存在于实际的磁盘介质或者文件系统）不同的是消息队列存放在内核中，只有在内核重启(即，操作系统重启)或者显式地删除一个消息队列时，该消息队列才会被真正的删除。消息队列可以实现消息的随机查询,消息不一定要以先进先出的次序读取,也可以按消息的类型读取.比 FIFO 更有优势。消息队列克服了信号承载信息量少，管道只能承载无格式字 节流以及缓冲区大小受限等缺点。

**信号量(Semaphores)** ：信号量是一个计数器，用于多进程对共享数据的访问，信号量的意图在于进程间同步。这种通信方式主要用于解决与同步相关的问题并避免竞争条件。

**共享内存(Shared memory)** ：使得多个进程可以访问同一块内存空间，不同进程可以及时看到对方进程中对共享内存中数据的更新。这种方式需要依靠某种同步操作，如互斥锁和信号量等。可以说这是最有用的进程间通信方式。

**套接字(Sockets)** : 此方法主要用于在客户端和服务器之间通过网络进行通信。套接字是支持 TCP/IP 的网络通信的基本操作单元，可以看做是不同主机之间的进程进行双向通信的端点，简单的说就是通信的两方的一种约定，用套接字中的相关函数来完成通信过程。

8. 计算机网络四层模型
    
    1. 应用层：主要提供两个终端设备上的应用程序之间信息交换的服务，它定义了信息交换的格式，消息会交给下一层传输层来传输。 我们把应用层交互的数据单元称为报文。
        
    2. 传输层：传输层的主要任务就是负责向两台终端设备进程之间的通信提供通用的数据传输服务。
        
    3. 网络层：网络层负责为分组交换网上的不同主机提供通信服务。 在发送数据时，网络层把运输层产生的报文段或用户数据报封装成分组和包进行传送。在 TCP/IP 体系结构中，由于网络层使用 IP 协议，因此分组也叫 IP 数据报，简称数据报。
        
    4. 网络接口层
        
    
      
    
9. 传输层协议 tcp udp区别
    

**是否面向连接**：UDP 在传送数据之前不需要先建立连接。而 TCP 提供面向连接的服务，在传送数据之前必须先建立连接，数据传送结束后要释放连接。

**是否是可靠传输**：远地主机在收到 UDP 报文后，不需要给出任何确认，并且不保证数据不丢失，不保证是否顺序到达。TCP 提供可靠的传输服务，TCP 在传递数据之前，会有三次握手来建立连接，而且在数据传递时，有确认、窗口、重传、拥塞控制机制。通过 TCP 连接传输的数据，无差错、不丢失、不重复、并且按序到达。

**是否有状态**：这个和上面的“是否可靠传输”相对应。TCP 传输是有状态的，这个有状态说的是 TCP 会去记录自己发送消息的状态比如消息是否发送了、是否被接收了等等。为此 ，TCP 需要维持复杂的连接状态表。而 UDP 是无状态服务，简单来说就是不管发出去之后的事情了（**这很渣男！**）。

**传输效率**：由于使用 TCP 进行传输的时候多了连接、确认、重传等机制，所以 TCP 的传输效率要比 UDP 低很多。

**传输形式**：TCP 是面向字节流的，UDP 是面向报文的。

**首部开销**：TCP 首部开销（20 ～ 60 字节）比 UDP 首部开销（8 字节）要大。

**是否提供广播或多播服务**：TCP 只支持点对点通信，UDP 支持一对一、一对多、多对一、多对多；

10. 应用层协议 http
    

应用层协议：

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=ZmNkN2Q1YTQzM2NmNWVhOTc1MTFiMDQxMTAxYTI5MTNfT0pGUGJrSHlwQjVpNEZhRjRUbGpqU1NDN0lhazE3V1ZfVG9rZW46UTlKMGJDV28xb1ZFQkl4YWkzT2NGYWRBbmhnXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

全称超文本传输协议（Hypertext Transfer Protocol）。顾名思义，HTTP 协议就是用来规范超文本的传输，超文本，也就是网络上的包括文本在内的各式各样的消息，具体来说，主要是来规范浏览器和服务器端的行为的。

并且，HTTP 是一个无状态（stateless）协议，也就是说服务器不维护任何有关客户端过去所发请求的消息。这其实是一种懒政，有状态协议会更加复杂，需要维护状态（历史信息），而且如果客户或服务器失效，会产生状态的不一致，解决这种不一致的代价更高。

11. http可以基于udp吗 有什么好处
    

HTTP 1.0、1.1 和 2.0 版本都是基于 **TCP**（传输控制协议）的

HTTP 3.0 是基于 **UDP**（用户数据报协议）的，它使用了一种称为 QUIC 的新协议来替代 TCP。QUIC 是由 Google 开发的，旨在减少延迟并提高传输效率。与 TCP 不同，QUIC 在 UDP 之上实现了多路复用和数据包丢失检测与重传，因此即使某个数据包丢失，也不会影响其他数据流。

12. 快速排序原理 时间复杂度 稳定性
    

从数列中挑出一个元素，称为"基准"（pivot）。

重新排序数列，所有比基准值小的元素摆放在基准前面，所有比基准值大的元素摆在基准后面（相同的数可以到任一边）。在这个分区结束之后，该基准就处于数列的中间位置。这个称为分区（partition）操作。

递归地（recursively）把小于基准值元素的子数列和大于基准值元素的子数列排序。

时间复杂度o(nlogn)，不稳定

稳定性：排序算法的稳定性是指在排序过程中，相同值的元素之间的相对顺序是否保持不变。如果一个排序算法能够保持任何两个相等的元素的相对位置不变，那么这个算法就是稳定的；反之，如果排序可能会改变相等元素的先后顺序，则该算法是不稳定的。

13. 树先中后序遍历
    
14. 手撕合并两个有序链表
    

## 二面

1. 自我介绍
    
2. 简历深挖主要是实习的东西
    
3. 简历上写了Kafka所以问了一下，觉得Kafka特别在哪里为什么需要他
    
4. 很大的数据但是只有很小的内存的机器怎么排序?答的就是先一段一段排序 map然后再用归并排序reduce?但是当时想到这么多数据该怎么在 Reduce的时候全部放进内存，有一点晕和面试官一起讨论，其实只要合并几个有序数组取前面的的一点点排序，然后重新总结了一下。spark和hadoop 区别
    
5. 手撕1个sql一个有小括号对
    
6. 有效括号对变成树 这个只说了一下思路(递归)
    

## 一面（没挂

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=M2ZiYTEyNDhmNmNkZGViZGY3YTgwMDgxMGE4NzQ0YWFfblRORGxobW5YNldaS091VVRBN09LWWJhanFBTEc3cjNfVG9rZW46R1RjRmJadzRHb3puNlV4czlDcmM0U0EybnliXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

## 一面（挂

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=NTYxMDA5NTRmMzI0MzcwNWRlMTgzZWUwMTgyZWYzYjNfRHhkYTJBNU5lUmY5d0FRWWhyMHVVMFpiOWtPenhCd21fVG9rZW46Q3dlamJDOU9zb2RGUmJ4b2hLOGNGa2JybkNoXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

## 一面（秋招

1. 项目内容介绍
    
2. 为什么要使用HBase
    
3. HBase底层架构的了解
    
4. Kafka在项目中的作用（RabbitMQ）
    
5. Kafka如何保证消息顺序，如何避免重复消费（RabbitMQ）
    
6. HashMap的底层结构
    

JDK1.8之前 数组+链表

JDK1.8之后 数组+链表+红黑树

7. Java创建线程的方式
    

https://mp.weixin.qq.com/s/NspUsyhEmKnJ-4OprRFp9g

### **1.1、继承Thread类**

这是最普通的方式，继承`Thread`类，重写`run`方法，如下：

暂时无法在飞书文档外展示此内容

### **1.2、实现Runnable接口**

这也是一种常见的方式，实现`Runnable`接口并重写`run`方法，如下：

暂时无法在飞书文档外展示此内容

想深入研究可以参考之前《Runnable分析》的文章。

### **1.3、实现Callable接口**

和上一种方式类似，只不过这种方式可以拿到线程执行完的返回值，如下：

暂时无法在飞书文档外展示此内容

想深入研究可以参考之前《Callable分析》的文章。

### **1.4、使用ExecutorService线程池**

这种属于进阶方式，可以通过`Executors`创建线程池，也可以自定义线程池，如下：

暂时无法在飞书文档外展示此内容

8. 线程池核心参数
    
    1. 核心线程数
        
    2. 最大线程数
        
    3. 存活时间：当线程数大于核心线程数时，多余的空闲线程存活的最长时间
        
    4. 时间单位
        
    5. 任务队列：用来储存等待执行任务的队列
        
    6. 拒绝策略：当提交的任务过多而不能及时处理时，我们可以定制策略来处理任务
        
    7. 线程工厂
        
9. 线程池的线程创建与销毁逻辑
    
    ![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=ODZhMmE4ZjMyOWQ0NmI2YzcyZmY2NzIyMzRmNGM1ZjlfRXFhakszMU5DUk9xWllPN2pNZnFtdXZqcEsxUlV6ZXJfVG9rZW46QVZmNGJySUhWb1I4ZFB4c2JzTWMwdUJVblJmXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)
    
    2. 如果当前运行的线程数小于核心线程数，那么就会新建一个线程来执行任务。
        
    3. 如果当前运行的线程数等于或大于核心线程数，但是小于最大线程数，那么就把该任务放入到任务队列里等待执行。
        
    4. 如果向任务队列投放任务失败（任务队列已经满了），但是当前运行的线程数是小于最大线程数的，就新建一个线程来执行任务。
        
    5. 如果当前运行的线程数已经等同于最大线程数了，新建线程将会使当前运行的线程超出最大线程数，那么当前任务会被拒绝，拒绝策略会调用`RejectedExecutionHandler.rejectedExecution()`方法。
        
    
      再提一个有意思的小问题：**线程池在提交任务前，可以提前创建线程吗？**
    
      答案是可以的！`ThreadPoolExecutor` 提供了两个方法帮助我们在提交任务之前，完成核心线程的创建，从而实现线程池预热的效果：
    
    8. `prestartCoreThread()`:启动一个线程，等待任务，如果已达到核心线程数，这个方法返回 false，否则返回 true；
        
    9. `prestartAllCoreThreads()`:启动所有的核心线程，并返回启动成功的核心线程数。
        
    
      线程池中的线程销毁通常遵循以下条件和逻辑：
    
    11. **空闲线程超时销毁**：
        
        1. 如果线程池中的非核心线程在任务完成后长时间处于空闲状态，并且空闲时间超过了 `keep-alive time`，那么线程池会销毁这些非核心线程。**核心线程不被销毁**，即使它们处于空闲状态。
            
        2. 在销毁之前，线程池会检查当前的线程数量。如果线程数大于核心线程数，则会销毁多余的线程。
            
    12. **线程池关闭时销毁线程**：
        
        1. 当调用线程池的 `shutdown()` 或 `shutdownNow()` 方法时，线程池会进行有序的关闭。它首先会阻止接受新的任务，并尝试让已经提交的任务完成。
            
        2. 当所有任务完成后，线程池会关闭并销毁所有线程。
            
            - **`shutdown()`**：会等待任务执行完毕后再关闭线程池。
                
            - **`shutdownNow()`**：会立即尝试中断当前正在执行的任务，并返回待处理的任务列表。
                
    13. **线程池超时处理**：
        
        1. 如果线程池长时间没有活动（没有任务提交），某些线程池实现（如 `ScheduledThreadPoolExecutor`）可能会在经过一定时间没有任务的情况下自动销毁线程。
            
10. AQS和ReentrantLock
    

AQS 核心思想是，如果被请求的共享资源空闲，则将当前请求资源的线程设置为有效的工作线程，并且将共享资源设置为锁定状态。如果被请求的共享资源被占用，那么就需要一套线程阻塞等待以及被唤醒时锁分配的机制

AQS 的 CLH 变体队列是一个双向队列，会暂时获取不到锁的线程将被加入到该队列中，CLH 变体队列和原本的 CLH 锁队列的区别主要有两点：

- 由 **自旋** 优化为 **自旋 + 阻塞** ：自旋操作的性能很高，但大量的自旋操作比较占用 CPU 资源，因此在 CLH 变体队列中会先通过自旋尝试获取锁，如果失败再进行阻塞等待。
    
- 由 **单向队列** 优化为 **双向队列** ：在 CLH 变体队列中，会对等待的线程进行阻塞操作，当队列前边的线程释放锁之后，需要对后边的线程进行唤醒，因此增加了 `next` 指针，成为了双向队列。
    

  

11. 垃圾回收器有哪些
    

**[Serial 收集器](https://javaguide.cn/java/jvm/jvm-garbage-collection.html#serial-%E6%94%B6%E9%9B%86%E5%99%A8)**

**ParNew 收集器**

**[Parallel Scavenge 收集器](https://javaguide.cn/java/jvm/jvm-garbage-collection.html#parallel-scavenge-%E6%94%B6%E9%9B%86%E5%99%A8)**

**[CMS 收集器](https://javaguide.cn/java/jvm/jvm-garbage-collection.html#cms-%E6%94%B6%E9%9B%86%E5%99%A8)**

**G1 收集器**

12. 介绍 CMS和 G1
    
13. 静态变量存储位置
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=MTUxZTUwZmE0NjE0NmI1ZjRmZmYyYzE5NWM4MjcyYmNfWFhPQlRqa0tmMzYwaEZaSklEdFoxTzBXajBsVW9LempfVG9rZW46Slp6ZGJXTWdnb2NKa0p4Z1VXYWNsZkNZbmNkXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

14. 内存区域中栈和堆的区别
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=ODQ5NjFiZDhjNGNkOWUwNjI4ZDM2ZGJmOWQ1NzVjN2NfSGh3UnB4R0NnVFZONlg5dWZ3VWpNbnJWbnc0U3A1TERfVG9rZW46REdjSWI5bU9Db3lLRHJ4Q21sUWNRVHJqbkRoXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

15. MySQL索引结构
    

分类+b+树

16. 判断索引是否生效的几个例子
    

  

17. RR幻读的解决方法
    

可串行化

临键锁：是查询时InnoDB根据查询的条件而锁定的一个范围，这个范围中包含有间隙锁和记录锁；临键锁=间隙锁+记录锁。

其设计的目的是为了解决Phantom Problem(幻读)，因此临键锁主要是阻塞也是insert，但由于临键锁中包含有记录锁，因此临键锁所锁定的范围内如果包含有记录，那么也会给这些记录添加记录锁，从而造成阻塞除insert之外的操作；

临键锁锁住的区间为：**记录+区间（左开右闭）**

18. 算法题:三数之和，使用排序和双指针。感觉写的又臭又长。
    

反问

1.主要做的是什么业务

2.技术栈主要有哪些

3.美团数据团队相比友商有哪些特点和优势

4.本次面试评价

## 一面

1.介绍-下Hadoop的HDFS

2.Secondary NameNode的作用是什么(容错)

3.分布式系统的不可能三角是什么，CAP理论(一致性 可用性 分区容错性)x

4.在分布式系统中贝叶斯理论是什么 x

场景题:淘宝店铺管理页面，有很多功能，存在很多数据。需要哪些实体(数据表)，它们的关系是什么

5.数据库的三范式

6.InnoDB范式建模和Kimball维度建模(指标和维度)x(星型模式(Star Schema)或雪花模式(Snowflake Schema)

7.数据库事务的四大特性

手撕代码:二叉树层序遍历 10min

8.系统开发的经验(没有 机器学习经验比较多)x

讲项目经历

9.针对不平衡数据集的处理方法(如骑手违规，特征提取 数据预处理)

讲本科毕设 5min

10.最大的难点

11.解释-下KNN和度量方式

12.了解数据引擎吗?x

13.数据特征比较多的时候怎么筛选(不是图片的情况下)

场景题:预估tb明天订单量，可以使用任何数据，会需要哪些特征和模型建模?如果不是按天预测，是预测下一个小时呢?(考虑XGboost加权集成:

## 美团数开一面

1. 说说实习，中间遇到了什么困难，是怎么解决的
    
2. 数据库隔离级别，脏读幻读不可重复读
    
3. 数据库索引有哪些类型，索引的底层数据结构是什么，索引的优点和缺点
    

按照底层存储方式角度划分：

- 聚簇索引（聚集索引）：索引结构和数据一起存放的索引，InnoDB 中的主键索引就属于聚簇索引。
    
- 非聚簇索引（非聚集索引）：索引结构和数据分开存放的索引，二级索引（辅助索引）就属于非聚簇索引。MySQL 的 MyISAM 引擎，不管主键还是非主键，使用的都是非聚簇索引。
    

按照应用维度划分：

- 主键索引：加速查询 + 列值唯一（不可以有 NULL）+ 表中只有一个。
    
- 普通索引：仅加速查询。
    
- 唯一索引：加速查询 + 列值唯一（可以有 NULL）。
    
- 覆盖索引：一个索引包含（或者说覆盖）所有需要查询的字段的值。
    
- 联合索引：多列值组成一个索引，专门用于组合搜索，其效率大于索引合并。
    
- 全文索引：对文本的内容进行分词，进行搜索。目前只有 `CHAR`、`VARCHAR`、`TEXT` 列上可以创建全文索引。一般不会使用，效率较低，通常使用搜索引擎如 ElasticSearch 代替。
    
- 前缀索引：对文本的前几个字符创建索引，相比普通索引建立的数据更小，因为只取前几个字符。
    

索引底层数据结构存在很多种类型，常见的索引结构有：B 树、 B+ 树 和 Hash、红黑树。在 MySQL 中，无论是 Innodb 还是 MyISAM，都使用了 B+ 树作为索引结构。

**索引的优缺点**

**优点**：

- 使用索引可以大大加快数据的检索速度（大大减少检索的数据量），减少 IO 次数，这也是创建索引的最主要的原因。
    
- 通过创建唯一性索引，可以保证数据库表中每一行数据的唯一性。
    

**缺点**：

- 创建和维护索引需要耗费许多时间。当对表中的数据进行增删改的时候，如果数据有索引，那么索引也需要动态地修改，这会降低 SQL 执行效率。
    
- 索引需要使用物理文件存储，也会耗费一定空间
    

  

4. 计网：TCP四次挥手的过程
    
    ![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=ODQ5ZmY3NmEwYmVjNjEzMGNkNGM4NTRmODk1YmUyNjlfaWM2RWplc0lNbzhaZ1RobGhZN0JKeE5qUlRwSDQ3VVRfVG9rZW46UFZiZGJ0SDNib2FBaGJ4TGNSTGN3T3NobjFlXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)
    
    2. **第一次挥手**：客户端发送一个 FIN（SEQ=x） 标志的数据包->服务端，用来关闭客户端到服务端的数据传送。然后客户端进入 **FIN-WAIT-1** 状态。
        
    3. **第二次挥手**：服务端收到这个 FIN（SEQ=X） 标志的数据包，它发送一个 ACK （ACK=x+1）标志的数据包->客户端 。然后服务端进入 **CLOSE-WAIT** 状态，客户端进入 **FIN-WAIT-2** 状态。
        
    4. **第三次挥手**：服务端发送一个 FIN (SEQ=y)标志的数据包->客户端，请求关闭连接，然后服务端进入 **LAST-ACK** 状态。
        
    5. **第四次挥手**：客户端发送 ACK (ACK=y+1)标志的数据包->服务端，然后客户端进入**TIME-WAIT**状态，服务端在收到 ACK (ACK=y+1)标志的数据包后进入 CLOSE 状态。此时如果客户端等待 **2MSL** 后依然没有收到回复，就证明服务端已正常关闭，随后客户端也可以关闭连接了。
        

**只要四次挥手没有结束，客户端和服务端就可以继续传输数据**

**为什么第四次挥手客户端需要等待 2*MSL（报文段最长寿命）时间后才进入 CLOSED 状态？**

第四次挥手时，客户端发送给服务端的 ACK 有可能丢失，如果服务端因为某些原因而没有收到 ACK 的话，服务端就会重发 FIN，如果客户端在 2*MSL 的时间内收到了 FIN，就会重新发送 ACK 并再次等待 2MSL，防止 Server 没有收到 ACK 而不断重发 FIN。

> **MSL(Maximum Segment Lifetime)** : 一个片段在网络中最大的存活时间，2MSL 就是一个发送和一个回复所需的最大时间。如果直到 2MSL，Client 都没有再次收到 FIN，那么 Client 推断 ACK 已经被成功接收，则结束 TCP 连接。

5. Java集合，ArrayList和LinkedList
    

- **是否保证线程安全：** `ArrayList` 和 `LinkedList` 都是不同步的，也就是不保证线程安全；
    
- **底层数据结构：** `ArrayList` 底层使用的是 **`Object`** **数组**；`LinkedList` 底层使用的是 **双向链表** 数据结构（JDK1.6 之前为循环链表，JDK1.7 取消了循环。注意双向链表和双向循环链表的区别，下面有介绍到！）
    
- **插入和删除是否受元素位置的影响：**
    
    - `ArrayList` 采用数组存储，所以插入和删除元素的时间复杂度受元素位置的影响。 比如：执行`add(E e)`方法的时候， `ArrayList` 会默认在将指定的元素追加到此列表的末尾，这种情况时间复杂度就是 O(1)。但是如果要在指定位置 i 插入和删除元素的话（`add(int index, E element)`），时间复杂度就为 O(n)。因为在进行上述操作的时候集合中第 i 和第 i 个元素之后的(n-i)个元素都要执行向后位/向前移一位的操作。
        
    - `LinkedList` 采用链表存储，所以在头尾插入或者删除元素不受元素位置的影响（`add(E e)`、`addFirst(E e)`、`addLast(E e)`、`removeFirst()`、 `removeLast()`），时间复杂度为 O(1)，如果是要在指定位置 `i` 插入和删除元素的话（`add(int index, E element)`，`remove(Object o)`,`remove(int index)`）， 时间复杂度为 O(n) ，因为需要先移动到指定位置再插入和删除。
        
- **是否支持快速随机访问：** `LinkedList` 不支持高效的随机元素访问，而 `ArrayList`（实现了 `RandomAccess` 接口） 支持。快速随机访问就是通过元素的序号快速获取元素对象(对应于`get(int index)`方法)。
    
- **内存空间占用：** `ArrayList` 的空间浪费主要体现在在 list 列表的结尾会预留一定的容量空间，而 LinkedList 的空间花费则体现在它的每一个元素都需要消耗比 ArrayList 更多的空间（因为要存放直接后继和直接前驱以及数据）。
    

6. 介绍一下你认识的排序算法
    

[十大经典排序算法总结](https://javaguide.cn/cs-basics/algorithms/10-classical-sorting-algorithms.html#%E7%AE%80%E4%BB%8B)

7. Java设计模式
    

[工厂设计模式](https://javaguide.cn/system-design/framework/spring/spring-design-patterns-summary.html#%E5%B7%A5%E5%8E%82%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F)

[单例设计模式](https://javaguide.cn/system-design/framework/spring/spring-design-patterns-summary.html#%E5%8D%95%E4%BE%8B%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F)

[观察者模式](https://javaguide.cn/system-design/framework/spring/spring-design-patterns-summary.html#%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F)

8. JVM内存区域，一个对象从创建到消亡在JVM中的过程
    

#### Step1:类加载检查

虚拟机遇到一条 new 指令时，首先将去检查这个指令的参数是否能在常量池中定位到这个类的符号引用，并且检查这个符号引用代表的类是否已被加载过、解析和初始化过。如果没有，那必须先执行相应的类加载过程。

#### Step2:分配内存

在**类加载检查**通过后，接下来虚拟机将为新生对象**分配内存**。对象所需的内存大小在类加载完成后便可确定，为对象分配空间的任务等同于把一块确定大小的内存从 Java 堆中划分出来。**分配方式**有 **“指针碰撞”** 和 **“空闲列表”** 两种，**选择哪种分配方式由 Java 堆是否规整决定，而 Java 堆是否规整又由所采用的垃圾收集器是否带有压缩整理功能决定**。

**内存分配的两种方式** （补充内容，需要掌握）：

- 指针碰撞：
    
    - 适用场合：堆内存规整（即没有内存碎片）的情况下。
        
    - 原理：用过的内存全部整合到一边，没有用过的内存放在另一边，中间有一个分界指针，只需要向着没用过的内存方向将该指针移动对象内存大小位置即可。
        
    - 使用该分配方式的 GC 收集器：Serial, ParNew
        
- 空闲列表：
    
    - 适用场合：堆内存不规整的情况下。
        
    - 原理：虚拟机会维护一个列表，该列表中会记录哪些内存块是可用的，在分配的时候，找一块儿足够大的内存块儿来划分给对象实例，最后更新列表记录。
        
    - 使用该分配方式的 GC 收集器：CMS
        

选择以上两种方式中的哪一种，取决于 Java 堆内存是否规整。而 Java 堆内存是否规整，取决于 GC 收集器的算法是"标记-清除"，还是"标记-整理"（也称作"标记-压缩"），值得注意的是，复制算法内存也是规整的。

**内存分配并发问题（补充内容，需要掌握）**

在创建对象的时候有一个很重要的问题，就是线程安全，因为在实际开发过程中，创建对象是很频繁的事情，作为虚拟机来说，必须要保证线程是安全的，通常来讲，虚拟机采用两种方式来保证线程安全：

- **CAS+失败重试：** CAS 是乐观锁的一种实现方式。所谓乐观锁就是，每次不加锁而是假设没有冲突而去完成某项操作，如果因为冲突失败就重试，直到成功为止。**虚拟机采用 CAS 配上失败重试的方式保证更新操作的原子性。**
    
- **TLAB：** 为每一个线程预先在 Eden 区分配一块儿内存，JVM 在给线程中的对象分配内存时，首先在 TLAB 分配，当对象大于 TLAB 中的剩余内存或 TLAB 的内存已用尽时，再采用上述的 CAS 进行内存分配
    

#### Step3:初始化零值

内存分配完成后，虚拟机需要将分配到的内存空间都初始化为零值（不包括对象头），这一步操作保证了对象的实例字段在 Java 代码中可以不赋初始值就直接使用，程序能访问到这些字段的数据类型所对应的零值。

#### Step4:设置对象头

初始化零值完成之后，**虚拟机要对对象进行必要的设置**，例如这个对象是哪个类的实例、如何才能找到类的元数据信息、对象的哈希码、对象的 GC 分代年龄等信息。 **这些信息存放在对象头中。** 另外，根据虚拟机当前运行状态的不同，如是否启用偏向锁等，对象头会有不同的设置方式。

#### Step5:执行 init 方法

在上面工作都完成之后，从虚拟机的视角来看，一个新的对象已经产生了，但从 Java 程序的视角来看，对象创建才刚开始，`<init>` 方法还没有执行，所有的字段都还为零。所以一般来说，执行 new 指令之后会接着执行 `<init>` 方法，把对象按照程序员的意愿进行初始化，这样一个真正可用的对象才算完全产生出来。

9. SQL：order_table，字段order_id，order_amount 找出前三大金额的订单
    

暂时无法在飞书文档外展示此内容

10. 前k个最小的元素，实质就是排序
    

作者：发动态中 链接：[https://www.nowcoder.com/discuss/733745912773701632?sourceSSR=search](https://www.nowcoder.com/discuss/733745912773701632?sourceSSR=search) 来源：牛客网

  

## 美团 （数据开发）-一面

1.算法：归并链表 2.编程语言：可变类型与不可变类型，tuple设计出来的作用是什么，深拷贝浅拷贝，闭包，继承与多态，面向对象与面向过程，Python内存管理机制 3.算法：归并排序，选择排序，冒泡排序，快速排序的过程，时间，空间复杂度，快排能只用o1的空间复杂度吗，二叉树，二叉查找树，二叉线索树，b树，查找复杂度，树的遍历，栈和队列，如何简单实现 4.数据开发：spark rdd是什么，spark任务执行的底层逻辑，join的底层逻辑， 数据仓库一般架构是什么样的

### 1. Spark RDD 是什么

**RDD（Resilient Distributed Dataset，弹性分布式数据集）**是 Spark 的核心抽象。它的特点包括：

- **不可变性**：一旦创建，RDD 中的数据就不能修改。每次转换都会生成一个新的 RDD。
    
- **分布式存储**：数据在集群中分布在多个节点上，从而支持大规模数据的并行处理。
    
- **容错性**：RDD 记录了它们的血统（lineage），即数据是通过哪些转换生成的。如果部分节点发生故障，可以根据血统信息重新计算丢失的数据。
    
- **惰性求值**：对 RDD 的转换操作是惰性求值的，只有在触发行动操作（action）时才真正执行。这种特性可以帮助 Spark 进行任务优化和 DAG 调度。
    

---

### 2. Spark 任务执行的底层逻辑

当提交一个 Spark 任务时，执行过程大致如下：

1. **DAG 构建** Spark 会根据代码中定义的转换（transformation）和行动（action）操作构建一幅有向无环图（DAG），表示各个操作之间的依赖关系。
    
2. **阶段划分（Stage）** 根据依赖关系，DAG 调度器会将任务划分成多个阶段（Stage）。通常，一个阶段内部的操作可以在同一节点上执行，而不同阶段之间可能需要进行数据 Shuffle 来满足依赖要求。
    
3. **任务划分（Task）** 每个阶段会被进一步划分成多个任务（Task），每个任务对应于一个数据分区的处理。任务由任务调度器分配到集群中的各个 Executor 上执行。
    
4. **数据 Shuffle** 当操作（如 join、groupBy 等）涉及到不同分区之间的数据整合时，会触发 Shuffle 过程，即数据在集群节点间重新分区。Shuffle 是性能瓶颈之一，需要尽量优化或避免。
    
5. **任务执行与容错** 各 Executor 并行执行任务。如果任务执行失败，Spark 可以依据 RDD 的血统信息对失败的任务进行重计算。
    

---

### 3. Join 的底层逻辑

Spark 中的 join 操作有多种实现方式，主要包括：

- **Shuffle Join** 默认情况下，如果两个 RDD 没有预先按照相同的 key 进行分区，Spark 会对两个数据集按照 join key 进行分区（通常通过 hash 分区或排序），然后在每个分区内进行 join。这种方式需要 Shuffle 数据，可能会带来较高的网络和磁盘 I/O 开销。
    
- **Broadcast Join** 当其中一个数据集比较小时，可以采用广播策略。即将较小的数据集广播到所有 Executor 上，从而在本地进行 join 操作，避免大规模 Shuffle。Spark 会自动选择这种策略（前提是满足一定的大小阈值）。
    
- **Sort-Merge Join** 如果两个数据集都已经排序且按照相同的 key 分区，Spark 可以直接利用排序后的数据进行 merge join，从而减少 Shuffle 的开销。这种方式适用于大数据量且分区策略已经优化的场景。
    

---

### 4. 数据仓库一般架构

数据仓库的架构主要有以下几种常见模式：

1. **传统三层架构**
    
    1. **数据源层**：涵盖企业内部外部各种数据源，如关系型数据库、日志系统、文件系统等。
        
    2. **数据集成层（ETL）**：通过抽取（Extract）、转换（Transform）、加载（Load）流程，将原始数据进行清洗、整合、转换后写入数据仓库。此层有时也称为数据中间层或数据湖层。
        
    3. **数据存储层**：采用多维数据模型（例如星型或雪花型模式）组织数据，常见的存储方式包括关系型数据库、MPP（大规模并行处理）数据库等。
        
    4. **数据展现层**：提供 BI 工具、报表、数据挖掘等接口，供业务用户查询、分析和决策支持。
        
2. **现代云数据仓库架构**
    
    1. **数据湖**：利用云存储（如 AWS S3、Azure Blob Storage）存储原始数据，支持结构化和非结构化数据。
        
    2. **数据仓库引擎**：如 Amazon Redshift、Google BigQuery、Snowflake 等云数据仓库产品，提供高性能查询和弹性扩展能力。
        
    3. **数据集成平台**：采用 ELT（抽取、加载、转换）方式，先将数据加载到数据湖，再进行转换处理后存入数据仓库。
        
    4. **数据访问层**：借助 BI 工具、数据可视化平台和报表系统，实现对数据的查询和分析。
        

这种架构能够有效整合各种数据源，保证数据质量，并提供高效的数据分析能力，既适用于传统业务场景，也适用于大数据和实时数据分析需求。

5.操作系统：并发和并行，协程是什么，用在什么场景

并发：宏观上同时运行（可以是不断切换交替运行），并发更多的是关于“处理多个任务”的能力，而不是“同时执行多个任务”的能力。

并行：微观上同时运行，在同一时刻，多个任务（进程或线程）在多个处理器核心上同时执行。并行要求硬件支持，如多核 CPU 或分布式计算环境。并行处理允许多个任务真正同时运行，从而提高系统处理任务的效率。

**协程**是一种轻量级的并发处理方式，适用于需要处理大量并发任务的场景，尤其是在 I/O 密集型应用中，能够提高系统的并发能力而不会消耗太多系统资源。

协程本质上是比线程更轻量级的执行单元，能够在一个线程内进行任务切换和调度。与线程不同，协程不需要上下文切换的开销，也不需要操作系统调度器的支持。 6.计网：五层模型，tcp/udp，三次握手，四次握手，能否两次握手，四次握手中间连接中断如何处网

五层模型：应用层、传输层、网络层、数据链路层、物理层

tcp和udp的区别

1. 可靠性：tcp通过拥塞控制、流量控制、重传机制等实现其可靠传输，udp不保证数据一定可达，以及远端主机收到tcp报文后需要给出确认
    
2. 有序性：tcp发送数据时会带有序列号，接收端可以对数据进行排序，udp不支持
    
3. 有链接：tcp需要通过三次握手和四次挥手建立/断开连接，udp不需要
    
4. 头部开销：tcp的数据头部包括校验和、序列化、窗口大小、标志位等信息，需要更大的开销
    
5. udp适用于视频聊天等即时性要求较高且能够容忍丢包的场景，但对于普通的消息聊天需要tcp保证消息的可达
    
6. 是否有状态：tcp是有状态的，会记录自己发送的消息的状态，以及维持复杂的连接状态
    
7. 传输形式：tcp面向字节流，udp面向报文
    
8. 是否提供广播或多播服务：tcp仅支持点对点通信，udp支持一对一、一对多、多对一、多对多；
    

三次握手

一次握手：客户端发送带有SYN（seq=x）标志的数据包给服务器，然后客户端进入syn-send状态，等待服务器确认

二次握手：服务器发送带有SYN+ACK（seq=y，ack=x+1）标志的数据包给客户端，然后服务器进入syn-recv状态

三次握手：客户端发送带有ACK（seq=y+1）标志的数据包给服务器，然后客户端和服务器进入established状态，完成

四次挥手

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=Mjg0Y2U4NjAyZjI5NGIxMjU5NDkzZDVlNzhlNDE5NDlfQ2VLMGN6UFA1TzBYUGR2bDBsREV3ZDU1eHZ2MEFKQlRfVG9rZW46R1haMGJjSjVHb1QyeTB4MlltZGNZdVF0bjFiXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

1. 客户端发送fin包（syn=x）给服务器，之后客户端进入fin-wait-1状态
    
2. 服务器收到fin后，发送ack响应给客户端（ack=x+1），服务器进入close-wait状态，客户端收到后进入fin-wait-2？
    
3. 服务器发送fin包给客户端（syn=y），进入last-ack状态
    
4. 客户端收到fin后，进入time-wait，发送ack（ack=y+1）给服务器，并需要等待2*msl（报文段最长寿命）后关闭
    

**能否两次握手**

不能两次握手，可能会导致重复建立连接，如果第二次握手发起端没有收到信息，但是接收端并不知道，然而此时连接已经建立，发起端认为连接没有建立，会重新进行第一次握手，然后建立连接，但是之前的连接永远不会断开

三次握手：第一次成功b已知a的发送能力和b的接收能力没问题，第二次成功a已知a和b的发送接收能力都没问题，但此时b并不确定自己是否成功发送，a是否成功接收，因此需要第三次握手

**四次挥手中间连接中断如何处理**

#### **客户端发送 FIN 包后没有收到 ACK 包**

如果客户端在发送 FIN 包后没有收到服务器的 ACK 包，通常会进行重试，直到收到确认或超时。如果客户端在等待中发生异常退出，TCP 连接通常会被认为已经关闭，客户端会自动进入关闭状态。此时，服务器如果未收到 FIN 包，可能会处于等待状态，等待客户端的关闭请求。

- **重试机制**：客户端会重试发送 FIN 包，直到超时或者连接恢复。如果服务器仍然无法响应，客户端可以尝试重新连接或者重新启动连接终止过程。
    

#### **服务器发送 FIN 包后没有收到 ACK 包**

当服务器发送 FIN 包后，如果在一定时间内没有收到客户端的 ACK 包，则服务器会根据连接的状态（例如，`LAST_ACK` 状态）进行处理。如果确认客户端已经死掉或者连接丢失，服务器将认为连接关闭。

- **超时和断开**：如果在超时窗口内没有收到 ACK 包，服务器会放弃等待并强制关闭连接。这时可能会发送一个 RST（重置）包来强制关闭连接。
    

美团 （数据开发）-二面 挖项目（不深） 1. 数仓有哪几层，建立表的时候有什么需要注意的问题吗，举个例子，有哪些需要规范化的地方，这样规范化有什么样的好处 2. MySQL和NoSQL，讲一讲区别，优劣势 3. 讲一种你熟悉的机器学习算法，（答：attention），讲一讲它的原理，举个例子说明一下，你怎样评价一个模型的表现 4. random forest和gbdt原理讲一下，优劣势是什么 5. Git冲突怎么办，你如何撤回Git操作 6. 聊一聊你对大语言模型的理解，它现在在哪些应用上落地，国内外技术上有什么样的差距 7. 聊一聊你在工作中如何提出一个方案，并选择确定一个方案的 8. 在项目中你是担任什么样的角色，你是如何跟进一个项目的进度的 作者：超级牛克 链接：[https://www.nowcoder.com/feed/main/detail/b842054c72ba43418fb2601a4b83423b?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/b842054c72ba43418fb2601a4b83423b?sourceSSR=search) 来源：牛客网

  

  

这个就不详细写面经了，纯照着简历上的项目问的，每次问你前会先说看到你的项目中用到了这个技术（施法前摇），然后再问你用这个技术在项目中解决了什么问题，之后将该技术延申到美团的业务中去，出场景题。一共三道场景题，分别是关于Hive动态分区、Flink双流join及优化还有Doris表使用什么模型，其实还有一道关于可视化工具的使用的场景题，但是这块没复习，老师也没再细问了（直接打断老师施法）。最后两道sql题，因为是用手机打的视频，所以没让我写代码，只说思路，都挺简单的，简单得感觉老师已经想挂了我了所以想让我赶紧答完

，第一道是group by再count，第二个是两张表join，写两个where条件之后再分组开窗。 作者：杨慕辣_许愿offer版 链接：[https://www.nowcoder.com/feed/main/detail/5dcb57fdbfe0450cb22285a0461a8c9e?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/5dcb57fdbfe0450cb22285a0461a8c9e?sourceSSR=search) 来源：牛客网

  

  

一面 项目拷打 spark数据倾斜 hive udf udaf udtf区别 flink水位线机制 java常用集合 java线程池创建线程 手撕sql不难 手撕最小前k个数（问能不能Arrays.sort，面试官说可以，他真的，我哭死😭） 一小时后直接二面 维度建模相关 spark的stage划分 对未来的思考 手撕sql 作者：真行啊你 链接：[https://www.nowcoder.com/feed/main/detail/9d2e55f6baa94b1fbbb8a4358bf23149?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/9d2e55f6baa94b1fbbb8a4358bf23149?sourceSSR=search) 来源：牛客网

# 饿了么面经

## 1.1

1.自我介绍 2.自我介绍中提到了保研 问了一下保研和奖学金 3.竞赛的主要分工 项目 4.rabbitmq如何保证数据按序到达

把需要保证顺序的数据发到1个Queue里去。 5.rabbitmq如何保证可靠性

丢失的三种情况：消息到 MQ 的过程中搞丢，MQ 自己搞丢，MQ 到消费过程中搞丢。

- 生产者到 RabbitMQ：事务机制和 Confirm 机制，注意：事务机制和 Confirm 机制是互斥的，两者不能共存，会导致 RabbitMQ 报错。
    
    - 事务机制：
        
    
      ● channel.txSelect(): 将当前[信道](https://so.csdn.net/so/search?q=%E4%BF%A1%E9%81%93&spm=1001.2101.3001.7020)设置成事务模式 ● channel.txCommit(): 用于提交事务 ● channel.txRollback(): 用于回滚事务
    
      通过事务实现机制，只有消息成功被rabbitmq服务器接收，事务才能提交成功，否则便可在捕获异常之后进行回滚，然后进行消息重发，但是事务非常影响rabbitmq的性能。还有就是事务机制是阻塞的过程，只有等待服务器回应之后才会处理下一条消息
    
    - conﬁrm模式（用的居多）
        
    
      一旦channel进入conﬁrm模式，所有在该信道上发布的消息都将会被指派一个唯一的ID（从1开始），一旦消息被投递到所有匹配的队列之后；rabbitMQ就会发送一个ACK给生产者（包含消息的唯一ID），这就使得生产者知道消息已经正确到达目的队列了；如果rabbitMQ没能处理该消息，则会发送一个Nack消息给生产者，生产者可以进行重试操作。
    
- RabbitMQ 自身：持久化、集群、普通模式、镜像模式。
    
    - 镜像模式：集群模式非常经典的就是Mirror镜像模式，保证100%数据不丢失，在实际工作中用的最多的。并且实现集群非常的简单，一般互联网大厂都会构建这种镜像集群模式。
        
- RabbitMQ 到消费者：basicAck 机制、死信队列、消息补偿机制。
    

6.消息队列还有什么用

解耦、异步、削（xue）峰填谷 提到了秒杀下单、削峰填谷、 7.秒杀中还会发生什么问题 库存超卖 一人一单解决方法（提到了分布式锁） 8.分布式锁怎么实现的、mysql中怎么实现分布式锁具体语句、还有什么分布式锁（z）

  **MySQL 实现**：

- 利用内置函数：`SELECT GET_LOCK('my_lock', 10);` 和 `SELECT RELEASE_LOCK('my_lock');`。
    
- 或基于表的方式，通过唯一键插入与删除来实现互斥访问。
    

  **其他实现**：

- **Redis**：使用 SET NX EX 命令（Redlock 算法）。
    

  锁其实也是一种资源，各线程争抢锁操作对应到redisson中就是争抢着去创建一个hash结构，谁先创建就代表谁获得锁；hash的名称为锁名，hash里面内容仅包含一条键值对，键为redisson客户端唯一标识+持有锁线程id，值为锁重入计数；给hash设置的过期时间就是锁的过期时间。

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=NWI3MzI4ZGM1YzNlOWViOWFkODYxOThjZThkMjNlNDlfZmxSdnlVU0R3VzVvRmJsN00wcFlIeDhYb3o0ZThvTGlfVG9rZW46VHhneGJwZ2ZCb2syQjB4emZaOWNWRGYxbnVlXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

- **Zookeeper**：利用临时顺序节点，常用 Curator 实现。
    

9.linux创建文件的命令、查看后台进程的命令（linux这一块回答的不太好）

1. 创建文件的命令
    
      **touch**： 快速创建一个空文件（如果文件已存在，则更新文件的修改时间）。
    
    2. `touch filename.txt`
        
    
      **cat**： 使用重定向操作创建文件并写入内容（不会打开编辑器）。
    
    3. `cat > filename.txt` `# 输入内容后，按 Ctrl+D 结束输入`
        
    
      **echo**： 通过 echo 命令创建文件并写入一行内容。
    
    4. `echo "Hello, Linux" > filename.txt`
        
    
      **编辑器创建**： 也可以使用编辑器（如 vi、vim、nano 等）来创建文件。
    
    5. `vi filename.txt`
        
2. 查看后台进程的命令
    
    1. **ps**： 列出系统中的所有进程，可使用多种参数来过滤和格式化输出。
        

`ps -ef # 显示所有用户的进程` `ps aux # 类似于 ps -ef，输出格式略有不同`

- **top 实时监控系统中的所有进程，包括后台进程。**
    

3. 查看具体线程的占用情况
    
    1. top
        
    
       `top -H -p <PID>` 命令，可以实时显示指定进程（PID）中所有线程的信息。
    
    2. ps
        
    
      `ps -T -p <PID>`
    

10.concurrentmap、hashtable和hashmap的区别

concurrentmap、hashtable线程安全

`ConcurrentHashMap` 和 `Hashtable` 的区别主要体现在实现线程安全的方式上不同。

- **底层数据结构：** JDK1.7 的 `ConcurrentHashMap` 底层采用 **分段的数组+链表** 实现，JDK1.8 采用的数据结构跟 `HashMap1.8` 的结构一样，数组+链表/红黑二叉树。`Hashtable` 和 JDK1.8 之前的 `HashMap` 的底层数据结构类似都是采用 **数组+链表** 的形式，数组是 HashMap 的主体，链表则是主要为了解决哈希冲突而存在的；
    
- **实现线程安全的方式（重要）：**
    
    - 在 JDK1.7 的时候，`ConcurrentHashMap` 对整个桶数组进行了分割分段(`Segment`，分段锁)，每一把锁只锁容器其中一部分数据（下面有示意图），多线程访问容器里不同数据段的数据，就不会存在锁竞争，提高并发访问率。
        
    - 到了 JDK1.8 的时候，`ConcurrentHashMap` 已经摒弃了 `Segment` 的概念，而是直接用 `Node` 数组+链表+红黑树的数据结构来实现，并发控制使用 `synchronized` 和 CAS 来操作。（JDK1.6 以后 `synchronized` 锁做了很多优化） 整个看起来就像是优化过且线程安全的 `HashMap`，虽然在 JDK1.8 中还能看到 `Segment` 的数据结构，但是已经简化了属性，只是为了兼容旧版本；
        
    - **`Hashtable`****(同一把锁)** :使用 `synchronized` 来保证线程安全，效率非常低下。当一个线程访问同步方法时，其他线程也访问同步方法，可能会进入阻塞或轮询状态，如使用 put 添加元素，另一个线程不能使用 put 添加元素，也不能使用 get，竞争会越来越激烈效率越低。
        

### HashMap 和 Hashtable 的区别

- **线程是否安全：** `HashMap` 是非线程安全的，`Hashtable` 是线程安全的,因为 `Hashtable` 内部的方法基本都经过`synchronized` 修饰。（如果你要保证线程安全的话就使用 `ConcurrentHashMap` 吧！）；
    
- **效率：** 因为线程安全的问题，`HashMap` 要比 `Hashtable` 效率高一点。另外，`Hashtable` 基本被淘汰，不要在代码中使用它；
    
- **对 Null key 和 Null value 的支持：** `HashMap` 可以存储 null 的 key 和 value，但 null 作为键只能有一个，null 作为值可以有多个；Hashtable 不允许有 null 键和 null 值，否则会抛出 `NullPointerException`。
    
- **初始容量大小和每次扩充容量大小的不同：** ① 创建时如果不指定容量初始值，`Hashtable` 默认的初始大小为 11，之后每次扩充，容量变为原来的 2n+1。`HashMap` 默认的初始化大小为 16。之后每次扩充，容量变为原来的 2 倍。② 创建时如果给定了容量初始值，那么 `Hashtable` 会直接使用你给定的大小，而 `HashMap` 会将其扩充为 2 的幂次方大小（`HashMap` 中的`tableSizeFor()`方法保证，下面给出了源代码）。也就是说 `HashMap` 总是使用 2 的幂作为哈希表的大小,后面会介绍到为什么是 2 的幂次方。
    
- **底层数据结构：** JDK1.8 以后的 `HashMap` 在解决哈希冲突时有了较大的变化，当链表长度大于阈值（默认为 8）时，将链表转化为红黑树（将链表转换成红黑树前会判断，如果当前数组的长度小于 64，那么会选择先进行数组扩容，而不是转换为红黑树），以减少搜索时间（后文中我会结合源码对这一过程进行分析）。`Hashtable` 没有这样的机制。
    
- **哈希函数的实现**：`HashMap` 对哈希值进行了高位和低位的混合扰动处理以减少冲突，而 `Hashtable` 直接使用键的 `hashCode()` 值。
    

扩展：`HashSet` 底层就是基于 `HashMap` 实现的。

11.stringbuilder和stringbuffer谁是线程安全的

stringbuffer是线程安全的，`StringBuffer` 对方法加了同步锁或者对调用的方法加了同步锁，所以是线程安全的。`StringBuilder` 并没有对方法进行加同步锁，所以是非线程安全的。 手撕 最长回文串[5. 最长回文子串 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-palindromic-substring/description/?envType=study-plan-v2&envId=top-100-liked) 反问环节 问大概多长时间出结果 反问完又问我 1.目前手头还有offer吗 2.硕士阶段有论文要求吗 3.后续规划是做java吗 作者：牛客680361917号 链接：[https://www.nowcoder.com/feed/main/detail/2426657c5739437896f54dc46562adc6?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/2426657c5739437896f54dc46562adc6?sourceSSR=search) 来源：牛客网

## 1.2

1. 自我介绍
    
2. 拷打实习
    
3. Java里的三种特性，分别是什么？这三种特性解决了我们面向对象设计的什么问题？那在多态在Java里的具体表现？
    

封装、多态、继承

**封装**：保护数据、隐藏实现细节，降低复杂性。

**继承**：实现代码复用和层次化设计。

**多态**：通过父类引用调用子类重写的方法，实现动态绑定，增强系统的灵活性与扩展性。

  **多态在 Java 中的具体表现**

1. **父类引用指向子类对象** 例如：
    

  `Animal animal = new Dog();` `animal.speak(); // 根据实际对象 Dog 调用 Dog 类中重写的 speak() 方法`

  在这个例子中，`Animal` 是父类，`Dog` 是子类。虽然变量类型是 `Animal`，但实际调用的是 `Dog` 中实现的方法，这就是多态的核心。

2. **方法重写（Override）** 子类对父类的方法进行重写，使得相同的方法调用在不同对象上产生不同的行为，实现运行时多态。
    
3. **编译时多态（方法重载）** 虽然方法重载（Overload）也被称为多态的一种，但它是在编译期通过方法签名的不同来决定调用哪个版本，这种多态是静态的。例如：
    

  `public void print(String s) { ... }` `public void print(int i) { ... }`

  编译器根据传入参数类型决定调用哪个方法。

4. 讲一下这个main函数执行后，JVM发生了什么
    
      **类加载**：
    
    2. `A` 类先被加载、验证、初始化，随后 `main` 方法开始执行；
        
    3. 在 `A.sout()` 中首次用到 `B` 时，`B` 才会被加载和初始化。
        
    
      **方法区、堆、栈**：
    
    5. **方法区（元空间）**：存放类的元信息、常量池、静态变量等；
        
    6. **堆**：存放对象实例，如 `new B()`、`new String("hello world")`；
        
    7. **虚拟机栈**：每个方法调用对应一个栈帧，存放局部变量、操作数栈等信息。
        
    
      **字符串字面量与 new String**：
    
    8. 字面量（如 `"hello word"`, `"hello world"）` 存在字符串常量池中；
        
    9. `new String("...")` 会在堆上创建一个新对象。
        
    
      **字节码指令**：
    
    10. 静态方法调用：`invokestatic`；
        
    11. 实例方法调用：`invokevirtual`；
        
    12. 对象创建：`new` 指令；
        
    13. 字符串常量：`ldc`（或类似指令）将常量池中的引用压栈。
        
5. 一个股票的题。
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=YWIzMjM5NGU0Y2JkNmU2YTgyM2RiY2E5Yzk0OWQzMDdfMzdPUXdJYzNoOTQwOGFMTXQ4bWphVE5ibjBTTzREcGpfVG9rZW46VElyMmJiUFFwb2hKRXd4TGdkSmNPdGlPbml4XzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

作者：Asp1rin 链接：[https://www.nowcoder.com/feed/main/detail/8bb9732d754d42e38f0fb555b18c4256?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/8bb9732d754d42e38f0fb555b18c4256?sourceSSR=search) 来源：牛客网

## 1.3

写面经攒人品。 3.27饿了么一面 自我介绍 介绍一下项目 使用了布隆过滤器，介绍布隆过滤器。 假如数据库更新，删除了几个数据，布隆过滤器怎么处理？回答的在外面套一层set，面试官继续追问还有其他方案没有，没想出来。 介绍延时双删 单例模式 懒汉模式 MySQL事务隔离等级，怎么实现的（一下子忘了第三级叫什么，还有mvcc） tcp三次握手，四次挥手，为什么等待2msl 使用https的全过程 拥塞控制 spring ioc原理，怎么实现的 反问： 主要负责什么业务 对实习生有什么要求 作者：啊喽哈哈哈哈 链接：[https://www.nowcoder.com/feed/main/detail/f6a77feb86664ec8a66ed062bb7d5675?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/f6a77feb86664ec8a66ed062bb7d5675?sourceSSR=search) 来源：牛客网

  

## 1.4

1.自我介绍 2.拷打项目(练手项目，问的很细致，包括数据库表有哪些，延伸如果要增加xx 功能怎么实现) 3.sql题目(写sql，这么写存在的问题，怎么解决) 4.cms vs g1 5.算法：判断B是否是A的子串 作者：牛客666655143号 链接：[https://www.nowcoder.com/feed/main/detail/fea3e1a6db92480b820a436b9e03af40?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/fea3e1a6db92480b820a436b9e03af40?sourceSSR=search) 来源：牛客网

## 1.5

1. tcp三次握手四次握手
    
2. https的网址键入浏览器后发生了什么
    
3. https四次握手
    
    1. 客户端请求建立SSL链接，并向服务端发送一个随机数–Client random和客户端支持的加密方法，比如RSA公钥加密，此时是明文传输。
        
    2. 服务端回复一种客户端支持的加密方法、一个随机数–Server random、授信的服务器证书和非对称加密的公钥。
        
    3. 客户端收到服务端的回复后利用服务端的公钥，加上新的随机数–Premaster secret 通过服务端下发的公钥及加密方法进行加密，发送给服务器。
        
    4. 服务端收到客户端的回复，利用已知的加解密方式进行解密，同时利用Client random、Server random和Premaster secret通过一定的算法生成HTTP链接数据传输的对称加密key – session key。
        
4. 怎么用netty建立（WebSocket）ws的 （然后问了两个netty的问题，netty的出入栈？？直接说不会）
    
5. ws建立过程
    
6. 布隆过滤器干什么的，原理
    

**作用**：布隆过滤器是一种空间效率高的概率数据结构，用来判断一个元素是否在一个集合中。它的主要优点是可以用很小的空间进行大规模数据的查找，但它会有一定的误判率（假阳性），即有可能判断某个元素在集合中，但实际上并没有。

**原理**：布隆过滤器使用多个哈希函数将元素映射到位数组中。当查询时，通过相同的哈希函数检查对应的位是否为 1。如果都为 1，则该元素很可能存在；如果有一个为 0，则该元素一定不存在。

7. 除了布隆过滤器还有什么方法解决缓存穿透
    

**空值缓存**：对于查不到的请求，缓存一个空值，防止重复访问数据库。 答了**逻辑过期（缓存击穿）**，追问还有吗，答一滴都没有了 （三剑客虽然经典但容易被追问，还得看看更多解决方案）

8. mysql隔离级别，mvcc
    
      **MySQL 隔离级别**：MySQL 提供四种隔离级别：
    
    2. **读未提交**：事务可以读取未提交的数据（脏读）。
        
    3. **读已提交**：事务只能读取已提交的数据，避免脏读，但可能出现不可重复读。
        
    4. **可重复读**：事务多次读取同一数据时，结果一致，避免脏读和不可重复读，但可能出现幻读。
        
    5. **串行化**：事务串行执行，避免所有并发问题，但性能差。
        
    
      **MVCC（多版本并发控制）**：通过为每行数据保存多个版本，确保事务之间不会相互干扰，每个事务读取数据时，都看到该事务开始时的数据版本，避免了脏读和不可重复读。
    
9. 想要快速找到几公里内的商家怎么做 答geohash，问有什么弊端 答：可能字符串比较长导致查询效率低？建立一下前缀索引 （实际应该是精度不够）
    

使用 **Geohash**：Geohash 将地理坐标（经纬度）映射为一个字符串，这些字符串可以进行字典排序，从而高效地进行区域查询。

**弊端**：Geohash 的精度有限，可能导致查询范围过大或过小，影响查询的准确性，精度不够可能会影响查询结果的精确度。

10. 深分页是什么
    

**深分页**：在数据量很大的情况下，传统的分页方法（如 `LIMIT` 和 `OFFSET`）在查询深度较大时，会导致性能下降，因为数据库需要跳过大量的数据来定位到正确的位置。

11. 如果是一个数据比较复杂的b端系统，并且用传统分业，怎么解决深分页
    

可以通过使用 **基于主键或索引的游标分页**（即通过上次查询的最后一条记录的 ID 来获取下一页的数据）来避免性能问题。

另外，使用 **搜索引擎**（如 Elasticsearch）来实现分页查询，也可以解决深分页问题。

12. b+树插入和删除一个元素，会发生什么
    

**插入**：如果插入的元素使某个节点满了，那么该节点会分裂为两个节点，并将中间值提升到父节点。如果父节点也满了，则递归进行分裂，直到根节点。如果根节点分裂，树的高度增加。

**删除**：删除一个元素后，如果某个节点的元素数量低于最小阈值，则会与相邻的兄弟节点合并，或者从兄弟节点借一个元素。如果合并后节点的元素数量仍低于最小阈值，则继续向上进行合并。

作者：希望offer多多的小鲸鱼很爱吃肉 链接：[https://www.nowcoder.com/feed/main/detail/d420cab3a128497a8647d350cdc0cf83?sourceSSR=dynamic](https://www.nowcoder.com/feed/main/detail/d420cab3a128497a8647d350cdc0cf83?sourceSSR=dynamic) 来源：牛客网

  

# 众安保险

## 1.1

1. ==，equals的区别，Interger比较是用==还是equal
    

对于基本类型数据，==比较的是值，`equals()` 不能用于判断基本数据类型的变量

对于引用类型数据，==比较的是对象的地址，通过重写equals函数实现对象属性值的比较

如果没有重写没有重写 `equals()`方法：通过`equals()`比较该类的两个对象时，等价于通过“==”比较这两个对象，使用的默认是 `Object`类`equals()`方法。

Integer比较用equals

2. 拆箱和装箱
    

如果将int类型的数据赋给integer，会自动装箱， 将integer赋给int会自动拆箱

- 装箱：将基本类型用它们对应的引用类型包装起来；
    
- 拆箱：将包装类型转换为基本数据类型；
    

3. 原子操作类atomic，CAS实现的原理，CAS不操作会返回失败吗
    

原子操作类atomic：提供了一种线程安全的方式来操作单个变量。`Atomic` 类依赖于 CAS（Compare-And-Swap，比较并交换）乐观锁来保证其方法的原子性，而不需要使用传统的锁机制（如 `synchronized` 块或 `ReentrantLock`）。

CAS（compare and swap）：首先比较读取到的值和预期值是否一致，如果一致则说明没有被修改过，如果不一致则不进行赋值操作。

`Unsafe#getAndAddInt`源码：

暂时无法在飞书文档外展示此内容

可以看到，`getAndAddInt` 使用了 `do-while` 循环：在`compareAndSwapInt`操作失败时，会不断重试直到成功。也就是说，`getAndAddInt`方法会通过 `compareAndSwapInt` 方法来尝试更新 `value` 的值，如果更新失败（当前值在此期间被其他线程修改），它会重新获取当前值并再次尝试更新，直到操作成功。

由于 CAS 操作可能会因为并发冲突而失败，因此通常会与`while`循环搭配使用，在失败后不断重试，直到操作成功。这就是 **自旋锁机制** 。

缺点：CAS 经常会用到自旋操作来进行重试，也就是不成功就一直循环执行直到成功。如果长时间不成功，会给 CPU 带来非常大的执行开销。

CAS不操作会返回失败吗 自旋重试

4. 操作数据库，mybatis的写法和流程思路，主要要注意哪些问题？
    

特别注意 **SQL 注入防护**、**事务管理** 和 **缓存配置** 等问题，确保数据安全和性能

5. a,b用户同时对一个状态进行操作，怎么处理？回答了数据库锁，synchronized,信号量，问还有什么方法。提示用redis（分布式锁，不会，问锁哪个字段）
    

  

6. mysql事务隔离级别，默认是什么
    

- 读未提交
    
- 读已提交
    
- 可重复读（默认）
    
- 串行化
    

7. spring的事务是怎么实现的，使用的时候要注意什么问题？(提示private方法事务失效怎么处理)
    

### Spring 支持两种方式的事务管理

#### 编程式事务管理

通过 `TransactionTemplate`或者`TransactionManager`手动管理事务，实际应用中很少使用，但是对于你理解 Spring 事务管理原理有帮助。

#### [声明式事务管理](https://javaguide.cn/system-design/framework/spring/spring-transaction.html#%E5%A3%B0%E6%98%8E%E5%BC%8F%E4%BA%8B%E5%8A%A1%E7%AE%A1%E7%90%86)

推荐使用（代码侵入性最小），实际是通过 AOP 实现（基于`@Transactional` 的全注解方式使用最多）。

暂时无法在飞书文档外展示此内容

事务传播：

**1.****`TransactionDefinition.PROPAGATION_REQUIRED`**

使用的最多的一个事务传播行为，我们平时经常使用的`@Transactional`注解默认使用就是这个事务传播行为。如果当前存在事务，则加入该事务；如果当前没有事务，则创建一个新的事务。

**2.****`TransactionDefinition.PROPAGATION_REQUIRES_NEW`**

创建一个新的事务，如果当前存在事务，则把当前事务挂起。也就是说不管外部方法是否开启事务，`Propagation.REQUIRES_NEW`修饰的内部方法会新开启自己的事务，且开启的事务相互独立，互不干扰。

**3.****`TransactionDefinition.PROPAGATION_NESTED`**:

如果当前存在事务，则创建一个事务作为当前事务的嵌套事务执行； 如果当前没有事务，就执行与`TransactionDefinition.PROPAGATION_REQUIRED`类似的操作。子事务也有自己的特性，可以独立进行回滚，不会引发父事务的回滚，但是前提是需要处理子事务的异常，避免异常被父事务感知导致外部事务回滚。

4.`TransactionDefinition.PROPAGATION_MANDATORY`

如果当前存在事务，则加入该事务；如果当前没有事务，则抛出异常。（mandatory：强制性）

#### `@Transactional` 的作用范围

1. **方法**：推荐将注解使用于方法上，不过需要注意的是：**该注解只能应用到 public 方法上，否则不生效。**
    
2. **类**：如果这个注解使用在类上的话，表明该注解对该类中所有的 public 方法都生效。
    
3. **接口**：不推荐在接口上使用。
    

#### `@Transactional` 的使用注意事项总结

- `@Transactional` 注解只有作用到 public 方法上事务才生效，不推荐在接口上使用；
    
- 避免同一个类中调用 `@Transactional` 注解的方法，这样会导致事务失效；
    
- 正确的设置 `@Transactional` 的 `rollbackFor` 和 `propagation` 属性，否则事务可能会回滚失败;
    
- 被 `@Transactional` 注解的方法所在的类必须被 Spring 管理，否则不生效；
    
- 底层使用的数据库必须支持事务机制，否则不生效；
    
- ……
    

8. AOP管理的是什么对象来保证原子操作？
    

动态代理，Spring AOP 主要是管理代理对象，通过切面（如事务增强器）把事务逻辑织入到目标对象中，从而借助事务管理器和数据库事务底层机制来保证原子操作的实现

9. 冒泡和快排的使用场景
    

冒泡：适用于数据量很小或者几乎有序的情况

快排：适用于中等到较大规模的数据排序

10. MySQL的索引了解哪些。where用函数导致索引失效的原因.
    

innodb采用b+树作为索引结构、聚簇索引

索引是按照列值的原始顺序组织和存储的。当对列应用函数时（如数学运算、字符串操作或日期函数等），函数会改变原始数据的值或格式，使得[数据库](https://cloud.tencent.com/product/tencentdb-catalog?from_column=20065&from=20065)无法直接定位到这些经过函数转换后的值。因此，数据库不得不执行全表扫描，以确保能够评估所有行上的函数操作，这导致查询性能下降。

11. 给一个订单，但是有多个商家，一个用户下单可能买了第一个、第三个商家，发货是分开发货，设计一个物流信息相关的表怎么设计
    

### 1. **表结构设计思路**

假设一个订单 `Order` 可能由多个商家参与，每个商家的商品可能会分开发货。我们可以设计以下几个表来实现这个需求：

1. **订单表 (****`orders`****)**：存储订单的基本信息。
    
2. **订单商品表 (****`order_items`****)**：存储订单中每个商家的商品信息。
    
3. **商家表 (****`merchants`****)**：存储商家的基本信息。
    
4. **物流信息表 (****`shipping_info`****)**：存储每个商品的物流信息。
    

### 2. **表结构设计**

#### 1. **订单表 (****`orders`****)**

- `order_id`：订单ID
    
- `user_id`：用户ID
    
- `order_status`：订单状态（例如：待支付、已支付、已发货、已完成等）
    
- `total_amount`：订单总金额
    
- `created_at`：订单创建时间
    
- `updated_at`：订单更新时间
    

暂时无法在飞书文档外展示此内容

#### 2. **订单商品表 (****`order_items`****)**

- `item_id`：订单商品ID
    
- `order_id`：关联订单ID
    
- `merchant_id`：商家ID，指示该商品属于哪个商家
    
- `product_id`：商品ID
    
- `quantity`：商品数量
    
- `price`：商品价格
    
- `item_status`：商品的状态（例如：待发货、已发货等）
    

暂时无法在飞书文档外展示此内容

#### 3. **商家表 (****`merchants`****)**

- `merchant_id`：商家ID
    
- `merchant_name`：商家名称
    
- `contact_info`：商家联系信息（可选）
    

暂时无法在飞书文档外展示此内容

#### 4. **物流信息表 (****`shipping_info`****)**

- `shipping_id`：物流信息ID
    
- `item_id`：关联的订单商品ID（与 `order_items` 表关联）
    
- `shipping_company`：物流公司
    
- `tracking_number`：物流单号
    
- `shipping_status`：物流状态（如：已发货、运输中、已送达等）
    
- `shipped_at`：发货时间
    
- `delivered_at`：送达时间
    

暂时无法在飞书文档外展示此内容

### 3. **设计说明**

1. **订单表 (****`orders`****)**：每个订单的基本信息都存储在 `orders` 表中。一个订单可能包含多个商家的商品，因此我们需要用 `order_items` 表来拆分每个商家的商品。
    
2. **订单商品表 (****`order_items`****)**：每个订单可以有多个商品（不同商家的商品），`order_items` 表中每个记录表示一个商家的商品，并且与 `orders` 表通过 `order_id` 关联。同时，`merchant_id` 指示该商品属于哪个商家，`item_status` 表示该商品的当前状态（如：待发货、已发货等）。
    
3. **商家表 (****`merchants`****)**：存储商家的基本信息。每个订单商品通过 `merchant_id` 和商家表进行关联，标识该商品来自哪个商家。
    
4. **物流信息表 (****`shipping_info`****)**：每个订单商品的物流信息单独存储。`shipping_info` 表中的每一条记录都与 `order_items` 表关联，表示该商品的物流信息，包括物流公司、物流单号和当前物流状态等。
    

5. RAG的实现过程 https://zhuanlan.zhihu.com/p/668082024
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=ZmVkMTNhMGZmYmU1YmIzMTdmYTI4YTcxYzVjNmVjOGRfcjYzTW9NUTB6YkhuamNRaGpFcTlrTXlKcXZmRnpadUxfVG9rZW46RGtQU2JGcEtUb2NpczV4UkdCcWNmQkxVbmFkXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

## 1.2

1. **Con**current**H**ashMap并发扩容过程
    

`ConcurrentHashMap` 的扩容只会扩容到原来的两倍。老数组里的数据移动到新的数组时，位置要么不变，要么变为 `index+ oldSize`，参数里的 node 会在扩容之后使用链表**头插法**插入到指定位置。

2. innodb为什么用b+不用b
    
    1. b+树的非叶子节点只存放索引信息，所以整体比b更矮胖，可以减少IO次数
        
    2. b+树的叶子节点是双向链表的结构，支持范围查询
        
    3. b+树的查询次数更稳定，只有查询到叶子节点才会结束，b树最好的情况只需要查询一次，最差的情况需要查询到叶子节点
        
3. A = 100 and B >= 200 and C = 300 indexA 和 index A B C怎么走索引为什么
    

MySQL 在选择索引时会遵循「最左前缀」+「遇到范围条件就停止」的规则。

1. 单列索引 `idx_A(A)`
    

- **能用上** 的：`A = 100`
    
- **后续条件**（`B >= 200`、`C = 300`）都 **不会** 再走索引，而是在回表后的行上再做过滤。
    

  **过程**：

2. 利用 `idx_A` 定位所有 `A=100` 的行的 _PK_（或者行位置）。
    
3. 根据这些行再去读表数据，检查 `B>=200 AND C=300`。
    

---

4. 复合索引 `idx_ABC(A,B,C)`
    

- **最左前缀**：第一列是 `A`，可用于 `A=100` 的**等值匹配**。
    
- **第二列**是 `B`，此处是 **范围查询** (`>=200`)。 一旦遇到范围查询，MySQL 会在 A=100 的分区上作一次 **区间扫描**（`B>=200`），然后 **停止** 使用这个索引的后续列。
    
- **第三列** `C` 虽然在索引里，但因为第二列已经是范围条件，所以不会再用来做索引定位，依然留到回表后再过滤。
    

  **过程**：

5. `A=100` → 定位到 `idx_ABC` 中所有键以 `(100,…)` 开头的条目范围。
    
6. 在这个范围中扫描满足 `B>=200` 的部分（使用索引的第二列做区间）。
    
7. 得到一系列行位置，再回表去检查 `C=300`。
    

8. kafka一个broker上多个topic数据是怎么存放的
    

  

9. 视频是如何和模型做交互
    
10. 聊聊llm
    

## 1.3 AIGC开发

1、自我介绍 2、讲一下实习过程中的难点（全局搜索功能 和 复杂 sql 优化） 3、现在有一张表，里面有千亿条数据，需要对整张表做一个聚合规则处理并且需要把结果写入到 excel 中（给出整体的设计包括技术细节和一下关键性的行为，不是通过 sql 语句实现） 4、有一张表，有个类型字段然后查询不同类型的数量（手撕 简单 sql）

暂时无法在飞书文档外展示此内容

5、把上面不同类型的数量的结果变成一行（select sum（case 类型 == ‘A’）as a1 ......得到不同类型的数量--这个结果是一个 n x n 的结果，n 是类型的数量，把这个语句作为一个子查询然后 max 不同字段即可）

暂时无法在飞书文档外展示此内容

## 1.4

1、自我介绍 2、问了我一些项目情况 3、你用Redis一般用来干嘛 4、Redis缓存机制，缓存穿透怎么解决

缓存穿透：要查询的值不存在数据库中

解决：布隆过滤器（接口层增加校验，如用户鉴权校验，id做基础校验，id<=0的直接拦截）、缓存空值、 5、如果每天有1000万条数据插入，如何设计数据库

6、分布式id如何生成？

雪花算法 7、你在你的项目中担任了什么角色？ 8、你在项目中遇到了什么问题，怎么解决的？包括技术上的问题？ 9、mysql主从库

MySQL的主从复制依赖于 binlog ，也就是记录 MySQL 上的所有变化并以二进制形式保存在磁盘上。复制的过程就是将 binlog 中的数据从主库传输到从库上。

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=NThiNTJjZjMwZWQ2YTQxMzgyYjQwYTM2N2RiN2E1MWZfTDBZOTV1TU95cDljZDBJMDN1emZlaGJWYkVKbzNHcmdfVG9rZW46TUlTTWJxYzM0b3BKdUV4aGVXRWM0Yzh2bkI0XzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

10、zookeeper, rocketMq会么 11、如果有大量请求，如何用多线程和消息队列及时响应？

多线程快速接收并入队，**后端**异步并行消费，让“接收请求”与“完成业务”解耦，既能保持高吞吐，又能及时给客户端响应。 12、线程池的核心线程数、线程池数量、等待队列怎么设置？

1. 核心线程数（corePoolSize）：
    

- CPU密集型：corePoolSize = CPU核数+1（避免过多线程竞争CPU）
    
- IO密集型：corePoolSize = CPU核数*2（或更高，具体看IO等待时间）
    

线程数 = CPU 核心数 * (1+ IO 耗时/CPU 耗时)

2. 最大线程数：
    

- **高并发系统**：对于需要处理大量并发请求的系统，可以适当增加最大线程数，以提高系统的并发处理能力。
    
- **资源受限的系统**：如果应用程序运行在资源受限的环境中，比如嵌入式系统或[云服务器](https://cloud.tencent.com/product/cvm?from_column=20065&from=20065)，通常需要限制最大线程数，以免过多线程占用资源。在这种情况下，最大线程数可以设置得相对较小。
    

3. 等待队列：
    

- **高并发系统**：对于高并发系统，可以考虑将任务队列设置得较大，以容纳更多等待执行的任务，从而降低任务被拒绝执行的概率。
    
- **资源受限的系统**：在资源受限的系统中，任务队列大小可能需要设置得相对较小，以控制内存消耗。
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=NWMzY2IxYTI5YjlkM2FiOTc5ZTUyNzZlYWVkNmNjOGZfbGNWcDR5eEg2blBkQ0xGdGNHSG4wTGFZOXJRc1Y0S1JfVG9rZW46RDZBZWJNRlJVb0JaeHR4anhrWGNxYThqbnpoXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=Zjg3NTRmNmU4NDkzNzM5NzUyYzc0M2YzYzJlNmE4M2VfN1RNUUFaUFFub0JxSXgxOFR3ZWpPbmFvOE1aazJQTGNfVG9rZW46VUFXQ2JiWDBhb3FHNUV4ejFSM2NkVENabnJlXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

  

# 客路

## 1.1

1. 进程，线程，协程的区别。
    
2. 如果一个表有一个性别字段，该给这个字段加索引吗？
    
3. 在linux系统中使用了ls命令出现异常，该怎么去纠错？
    

不清楚到底是什么异常 建议百度哈哈哈哈哈开玩笑 不会

4. 面向过程和面向对象的区别？
    

  

5. 讲讲实习的项目，难点。
    

## 1.2

1. 实现队列会用什么数据结构，会提供什么方法
    

数组或者链表

普通队列：入队、出队、队列大小、队头元素、队列是否为空

双端队列：offerfirst，offerlast，pollfirst，polllast

2. 多线程场景下怎么保证此队列的安全
    

- 最简单：在方法上加`synchronized`
    
- 用 `java.util.concurrent.locks.ReentrantLock` 做更灵活的互斥控制
    
- 用条件变量（`Condition`）实现阻塞队列
    
- 使用 JDK 并发队列（生产环境首选）
    
      **非阻塞无界队列**：`java.util.concurrent.ConcurrentLinkedQueue<T>`
    
      **有界阻塞队列**：`java.util.concurrent.ArrayBlockingQueue<T>`、`LinkedBlockingQueue<T>`
    

3. 添加什么要素来实现阻塞队列
    

- 容量控制（capacity）
    
- 同步原语 + 等待/唤醒机制
    

4. 介绍一下自己擅长的（讲了分布式相关的，TiKv）
    
5. Lru算法的实现
    
6. 聊了一下之前是否实习过，还有实验室项目类型（交流很愉快）
    
7. 反问环节问了下klook的业务还有业务中实现分布式事务的方法，也讲了一下自己对于这块的理解。 作者：Aputonos 链接：[https://www.nowcoder.com/feed/main/detail/b991f64996744f369b266b03a0ec44ed?sourceSSR=search](https://www.nowcoder.com/feed/main/detail/b991f64996744f369b266b03a0ec44ed?sourceSSR=search) 来源：牛客网
    

## 1.3

限制你半小时内做完6道题，题目做对了，就走技术面了。

1. 你在开发中碰到什么技术难点；
    
2. 有个兔子的问题，一对兔子第三个月后就开始繁殖，不考虑死亡，求兔子数。 答：其实就是一个斐波那契数列,我是用循环解决的，当然你也可以用递归： func rabbitCount(m int) int { sum:= 1 for m >=3 { m-=3 sum+=m } }；
    
3. 百万级Mysql怎么处理。 答：1-主备 2-读写分离 3-集群 4-负载均衡 5-查询缓存 6-raid
    
4. 怎么用go实现一个栈。 答：其实就是一个切片的问题，注意pop的时候一定要注意切片长度检查，否则会panic。
    
5. 有个考察defer和panic执行顺序的问题。 答：defer是先注册后调用(最后注册的最先被执行)。所有defer都执行完后，才会显示panic里面的东西。
    

作者：牛客152272034号 链接：[https://www.nowcoder.com/discuss/353159217387347968?sourceSSR=search](https://www.nowcoder.com/discuss/353159217387347968?sourceSSR=search) 来源：牛客网

## 1.4

一面

1. 自我介绍
    
2. HashMap底层数据结构
    
3. HashMap的扩容机制
    
4. HashMap是否是线程安全的
    
5. Java如何实现线程池
    
6. 进程线程的区别
    
7. 进程间通信
    
8. 什么是管道
    
9. redis的set和zset的区别
    
10. zset底层数据结构
    
11. 虚拟内存
    
12. innodb底层数据结构
    
13. 数据库死锁
    
14. innodb的上锁
    
15. 讲解实习经历
    

二面

1. 自我介绍
    
2. 希望加入一个什么样的公司
    
3. ==和equals区别
    
4. 为什么重写equals方法要重写hashcode方法
    
5. Java垃圾回收算法
    
6. select和epoll
    
7. 你觉得Java开发工程师要具备的能力，这几方面你做的怎么样
    
8. 你觉得我的方案有问题，如何处理这个情况
    
9. 讲解实习经历
    

  

# 虾皮

## 1.1

1. 自我介绍
    
2. **你常用哪个 JDK 版本？1.8**
    
3. **选一个你熟悉的项目，说说它配了什么垃圾回收器？**
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=ODI2ZDg2ODM4OTg1MDA5NzAxZjQ3OWRiYjVlNmI2NjZfSjRmcmVudHNDaWsxYzc2QVlRNVdpQjFyUGdDUTlpVXRfVG9rZW46T0gwMGJTaG5ub3lMelp4TGhpOGNqckJrbk5iXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

4. **描述一下 CMS 的回收流程及每个阶段的作用。**
    

- 初始标记：stw，遍历GC root，标记**直接与 root 相连**的对象
    
- 并发标记：标记可达对象，但此时并发的，所以无法完全标记
    
- 重新标记：stw，为了修正并发标记期间因为用户程序继续运行而导致标记产生变动的那一部分对象的标记记录
    
- 并发清除：GC 线程开始对未标记的区域做清扫
    

**浮动垃圾**：并发阶段新死亡的对象可能到下一轮才回收。

5. **CMS 有哪些阶段会 Stop-The-World？**
    

初始标记和重新标记

6. **新生代和老年代的划分机制是什么？**
    

**生存周期**，或者说是**经历GC的次数**，对象创建时，一般在新生代申请内存，经历一次GC年龄+1，当年龄超过一定值（默认15，可以通过参数 -XX:MaxTenuringThreshold 来设定），会进入老年代

7. **对象从新生代晋升到老年代的条件有哪些？**
    

https://blog.csdn.net/wangshiwen011/article/details/107473814

- 15次GC
    
- 假如说当前放对象的Survivor区域里，一批对象的总大小大于了这块Survivor区域的内存大小的50%，那么此时大于等于这批对象年龄的对象，就可以直接进入老年代了。
    
- 大对象直接进入老年代
    

8. **说说 AOP 的最基本实现方式。**
    

静态代理、动态代理

9. **Spring AOP 生成代理的两种场景/方式分别是什么？**
    

jdk和cglib

10. **你在项目里用过微服务吗？用的什么技术栈？**
    

  

11. 有做过哪些 MySQL 调优？举一个具体例子。
    

制造供应链平台里有个“货位列表”页面，支持按货位类型/是否冻结/库存是否为零筛选，并需要展示**每个货位的总现有量、物料信息、订单信息等**。我在库存货位列表查询上做过一次调优，上线后发现延迟较大。

12. **当时如何定位到这条慢 SQL？用了哪些工具或命令？**
    

打开慢日志（`long_query_time=1s`），用 `pt-query-digest` 找 Top 慢 SQL。

`EXPLAIN` 具体语句

举例：

**慢 SQL（简化版）**

暂时无法在飞书文档外展示此内容

**EXPLAIN 关键信号**

- 对 `mes_onhand_qty` 的子查询 **Using temporary（临时表）; Using filesort（文件排序）**；
    
- `t2` 走 **ALL** 或 **range**，`rows` 估计高（上百万），函数 `IFNULL()` 让本可用的索引**失效**；
    
- **`ORDER BY t1.location_code`** **没有能覆盖的组合索引，导致额外排序。**
    

修改：

`IFNULL(t2.onhand_qty,0) > 0` 修改为 `AND (? = 0 OR (t2.onhand_qty > 0 AND t2.delete_flag = 0))`

13. 解释 Explain 结果中哪些字段说明索引未生效。
    

**`type=ALL`****、****`key=NULL`****、****`rows`** **巨大、****`filtered`** **很低、****`Using filesort/temporary`****、隐式类型转换/函数导致索引失效、ICP/覆盖索引识别。**

14. Redis 持久化有哪些可选策略？
    

aof和rdb

15. RDB 与 AOF 各自有哪些刷盘/触发方式？
    

aof：always、every、no

rdb：`save`、`BGSAVE`

16. 如果要求“不丢数据”或“高性能”分别怎样配置？
    

混合持久化

17. TCP 四次挥手中 TIME_WAIT 状态的含义是什么？
    
    1. **确保最后的ACK报文能够被对方接收**：如果客户端A发送的最后一个ACK报文丢失，服务器B会重传FIN报文。此时，客户端A可以重新发送ACK报文，确保连接正常关闭。
        
    2. **防止旧连接的数据包干扰新连接**：在2MSL（maximum segment lifetime）时间内，旧连接的数据包会在网络中消失，避免新连接受到旧数据包的干扰。
        
18. 为什么需要等待 2MSL 才能进入 CLOSED？
    

一般是客户端发起，如果客户端的第二个报文发送失败，会在2MSL内收到服务器的请求从而进行重发，防止客户端已下线导致服务器无尽的等待，浪费资源

19. **你在项目里用过 CompletableFuture 吗？**
    
20. **它与 Future / Task 组合相比有什么优势？**
    
21. **如果异步链路里包含数据库事务，事务回滚会不会有问题？为什么？**
    
22. 线上 Linux 服务器常用命令熟悉吗？
    
23. 如何在文件里查找关键字？
    
24. 如何查找某个文件？
    
25. 手撕：求最长有效括号子串。
    

## 1.2

1. 自我介绍
    
2. 僵尸进程怎么产生、怎么处理（不会，直接过）
    

“僵尸进程”（即子进程结束后（自己执行力exit（）结束会释放资源，但不会释放pcb） PCB 仍然存在但父进程没有调用 wait()或 waitpid()等系统调用来获取子进程的状态信息），父进程即时调用 wait()或 waitpid()系统调用来回收子进程。

孤儿进程：子进程的父进程已结束，但未调用 wait()或 waitpid()回收子进程。解决：为避免占用资源，将孤儿进程的父进程设为init进程，由其进行子进程资源回收

3. 进程间通信方式（这个倒是背过）
    

匿名管道（可用于父子进程或兄弟进程）、有名管道、共享内存、信号量、套接字、消息队列、信号

4. time_wait状态八股（从四次挥手讲到解决过多time_wait）
    
5. 拥塞控制算法（楼主想从tcp tahoe、cubic说到bbr，但在bbr被无情打断，楼主准备了这么久的http3面经无处施展）
    

**慢开始、 拥塞避免、快重传 和 快恢复**

6. mysql select的流程、涉及的模块（楼主没懂什么叫涉及的模块，先说了一下算子，被打断说不对，说了点server跟innodb，感觉驴唇不对马嘴，面试官直接跳过了，只能说楼主跟面试官看的不是一份面经）
    
7. 索引失效的场景（没背过，跟面试官说索引的key怎么排列的，mysql是cbo，最左匹配原则不一定时刻成立被打断了）
    
8. 怎么看数据库的监控（知识盲区了，印象中这应该是dba干的活）
    
9. 如何评估系统稳定性（不知道啊，楼主答了个压测就没下文了）
    
10. 假如要给线上表加一个字段，怎么评估影响，稳定性（不知道啊，没做过啊，楼主说先在开发环境加好再找个流量低一点的时间上线，或者两个库，先把流量打到另一个库，这个库做升级，想想就很麻烦，面试官仍然不满意）
    
11. 线上环境cpu占用飙升，一分钟内该做什么（面试官一开始没提到服务器已经挂了，楼主说只能多做前期工作保留log以方便后续排查，然后面试官说看pprof，楼主反驳说直接给线上服务上性能监控软件导致服务崩了怎么办，面试官才说服务已经崩了，那有什么好说的嘛）
    

https://www.cnblogs.com/dennyzhangdd/p/11585971.html

12. 算法：快速排序
    

# 8.22 美团ai面

1. 自我介绍
    
2. HTTP和HTTPS的区别
    
    1. 对称和非对称加密为什么这样设计
        
    2. https握手流程，如何保证安全性的
        
3. Java的封装、继承、多态三种特性怎么理解？封装的安全性是如何实现的？如果所有属性均提供了get/set方法，还能否保证安全性？什么时候属性是可以公开的？
    
4. 缓存穿透会引起什么问题？怎么解决？布隆过滤器是如何解决这个问题的
    
5. 场景：智能家居场景下，设计一个多条件的多流程的操作，给出思路和数据结构？怎么获取不同的策略？如果是不同的时间，不同的状态怎么进行策略选择
    
6. ai的应用，实现用户只提供图片和关键词就能实现探店初稿，怎么实现？怎么能够覆盖图片和关键词的所有内容？怎么避免幻觉？
    
7. 开发中遇到的困难？如何解决的？如何发现问题和确定解决方案的？
    

## ai回答

下面按题目逐条给出“面试可说的要点 + 可落地做法”。

1. HTTP vs HTTPS
    

- **差异**：HTTPS=HTTP+TLS。多了**机密性（加密）**、**完整性（MAC/AEAD）**、**身份认证（证书）**；代价是握手与证书成本。
    
- **为何“非对称+对称”混用**
    
    - 非对称（RSA/ECDSA/ECDHE）：**建立密钥/认证身份**，计算贵；
        
    - 对称（AES-GCM/ChaCha20-Poly1305）：**批量数据加解密**，快。 ⇒ 用“非对称”安全协商“对称密钥”，再用“对称”传应用数据。
        
- **TLS 1.3 握手（简化）**
    
    - ClientHello：支持套件、随机数、SNI、ALPN、**KeyShare(ECDHE)**。
        
    - ServerHello(+Certificate, CertificateVerify, Finished)：给证书链与对 **握手数据签名**，并返回自己的 ECDHE。
        
    - 客户端验链&验签→用 ECDHE 算共享密钥→HKDF 派生会话密钥→发 Finished。
        
    - 之后用对称 AEAD 加密传输。 **安全性**：服务器身份靠证书与签名；机密/完整性靠 AEAD；**前向安全**靠“临时 ECDHE”；重放靠随机+序列号+一次性密钥；可配 HSTS 防降级/劫持。
        

2. Java 面向对象三特性
    

- **封装**：用 `private/protected` 隐藏状态，仅通过受控方法暴露；保障“不变式”（如金额≥0）。
    
    - 安全实现：校验入参、**不可变对象**（`final` 字段）、**防御性拷贝**（返回 `new ArrayList<>(list)`）；限制可变暴露。
        
    - “全 get/set 还能安全吗？”——不一定。无校验的 setter = 公开字段；应：只暴露必要 getter，**去掉宽松 setter**/改为**业务方法**（如 `deposit(amount)`）。
        
    - 何时可公开字段：**常量**(`public static final`)、**不可变值对象**的小型数据载体（record）且无不变式破坏风险。
        
- **继承**：`extends` 复用/抽象；注意里氏替换，优先**组合>继承**。
    
- **多态**：父类型引用指向子实现，运行时动态绑定；便于策略/模板等模式。
    

3. 缓存穿透
    

- **问题**：请求的 key **根本不存在**，每次都穿透到 DB，形成**高 QPS打DB**。
    
- **治理**：
    
    - **缓存空值**（短 TTL）防重复穿透；
        
    - **参数校验/鉴权/限流**；
        
    - **布隆过滤器**：用位图+多哈希，先判“可能存在/一定不存在”。不存在→直接拦截；可能存在→再查缓存/DB。
        
        1. 特性：**无假阴性**，有**假阳性**（可调 m、k 控制误报率）；写入时同步将 key 放入 Bloom。
            

4. 智能家居：多条件多流程的编排
    

- **核心抽象（规则引擎/状态机）**
    

暂时无法在飞书文档外展示此内容

- **执行思路**：事件总线→规则匹配（触发+条件）→冲突决策（优先级/互斥/最后写胜）→动作编排（并/串；重试/回滚）。
    
- **策略获取/选择**：
    
    - **按维度索引**（时间窗、状态、地理/房间、用户偏好）；
        
    - **状态机**（如 `HOME/AWAY/SLEEP`）+ 决策表（`state × time × env`→策略ID）；
        
    - 可插**策略模式**：`Strategy key = hash(state,time,env)` 动态路由。
        
- **不同时间/状态**：在规则里加 `validWindow` 与 `sceneState` 过滤；同设备冲突按 `priority` 或“最近手动优先”解决。
    

5. 数据库 ↔ 缓存一致性（补充与穿透关联）
    

- 读：Cache-Aside；miss→DB→回写缓存。
    
- 写：**先写DB，再删缓存**（必要时**延迟双删**或**事务消息/Outbox** 失效通知）。
    
- 强一致读：直接读主或短 TTL + 逻辑过期 + 互斥重建。
    

6. 大模型用于“探店初稿”（图+关键词）
    

- **目标**：用户给**图片+关键词**，产出结构化“探店初稿”，覆盖所有输入信息，少幻想。
    
- **流程**
    
    - **感知层**：多模态模型做 **OCR/Logo/菜品/场景**识别 + 图片描述；关键词做**意图/要点**抽取。
        
    - **检索增强（RAG）**（可选）：根据识别到的店名/地理线索去知识库/地图/菜单库取**可验证事实**（营业时段、均价、地址）。
        
    - **结构化生成**：用**模板+约束**生成（如 JSON Schema：店名/人均/环境/口味/推荐/注意事项/图片佐证段落）。
        
    - **覆盖校验**：把“图片要点+关键词列表”转为**勾选清单**，让模型在输出中逐项打勾；缺项→产出“待确认/TODO”，**宁缺毋造**。
        
    - **去幻觉**：
        
        1. **仅允许引用“输入或检索结果”**中的字段；
            
        2. 对每条陈述打上 `source=["img#1:ocr","kw:氛围", "retrieval:地图"]`；
            
        3. 对不确定信息输出“未知/需核实”，禁编造价格/地址。
            
    - **人审/A/B**：编辑器里二次修改；收集转化指标回训提示词。
        
- **关键技术**：多模态 LLM（图文理解）、向量检索、内容安全/合规词表、模板化 Prompt、JSON 输出校验与**拒答机制**。
    

---

**小抄总结**

- HTTPS：非对称做认证与密钥交换，对称做数据加密；TLS1.3 用 ECDHE+证书+AEAD+HKDF，具前向安全。
    
- OOP：封装靠访问控制/不可变/防御拷贝/业务方法；全量 getter/setter 可能破坏不变式。
    
- 穿透：不存在键打爆DB；空值缓存+布隆过滤+校验限流。
    
- 智能家居：规则引擎/状态机 + 决策表 + 优先级互斥；按时间/状态路由策略。
    
- 探店初稿：多模态识别 →（可选）检索 → 结构化生成 + 覆盖清单 + 溯源，避免幻觉。
    

# 美团面经

## 1.1

1. 你用了DDD，讲一下DDD的优势？
    
2. 什么是充血模型？
    
3. 你项目中的DDD具体怎么设计的？
    
4. DDD领域划分的依据？
    
5. 用了springboot，说一下springboot的原理？自动装配？
    
6. threadlocal实现原理？内存泄漏情况？怎么解决？
    
7. rabbitmq有哪些工作模式？
    
8. tcp三次握手和四次挥手？如果建立了连接后服务器出错了怎么办？
    
9. Redisson原理？会有什么问题？
    
10. Redis数据结构，用到了哪些？GEO只单纯的用了这个数据结构吗，还是有一些其他的操作？
    
11. Redis和数据库的一致性怎么保证的（这里我结合项目和面试官解释了10多分钟没解释清楚……）
    
12. 介绍一下Java虚拟机？说一下新建对象的过程？类信息存在堆里面吗？jdk7和jdk8方法区不同的区别？元空间对外还是对内的？如果对象太大会不会发生内存不够的情况？
    
13. MyBatis的底层原理（我这里没准备，直接回答不会）
    
14. MySQL的原理，InnoDB的原理，B+树的原理？
    
15. ConcurrentHashMap原理？
    
16. 可以说说你最近在学什么吗？有遇到什么挑战吗？
    

还有好多想不起来了

然后还有一大半的时间在拷打项目…巨深的那种

手撕：hot100贪心第一题 买股票

## 1.2

1 描述最左匹配原则并举例说明失效场景

2 聚簇索引与普通索引的区别

3 聚簇索引的缺点

4 聚簇索引叶子节点存什么

5 ES与MySQL的区别

6 ES的基本原理

7 缓存穿透、击穿、雪崩的概念及区别

8 缓存穿透的解决方案

9 布隆过滤器的底层原理

10 哈希函数越多越好吗

11 Redis如何实现分布式锁

12 除Redis外还能用什么实现分布式锁

13 Redisson是什么

14 Redisson相比原生Redis加锁的优势

15 Redis数据过期策略

16 Redis集群模式有哪些

17 主从模式有哪些形式

18 CompletableFuture与Future的区别

19 CompletableFuture常用的两个方法及区别

20 不传线程池时CompletableFuture默认使用什么

21 线程池核心参数如何设置

22 线程池任务执行流程

23 动态线程池了解吗

24 压测在性能调优中的作用

25 常用的并发安全容器有哪些

26 ConcurrentHashMap如何保证线程安全

27 HashTable与ConcurrentHashMap区别

28 synchronized与Lock的区别

29 synchronized与Lock谁更优

30 synchronized可以实现锁升级吗

31 volatile的作用

32 常用设计模式有哪些

33 单例模式在哪些场景使用

34 最常用的单例实现方式（建议DCL）

35 手写单例（懒汉+双检锁）

36 单例中volatile的作用

37 Java GC存在的意义

38 垃圾对象的判定标准

39 分代收集机制中Eden与Survivor的作用

40 Survivor区比例

41 动态年龄判断机制

42 Sentinel实现限流的注解/方式

43 限流与熔断的区别

44 Caffeine的核心方法

45 Caffeine相比其他本地缓存的优势

46 MySQL性能调优流程

47 消息队列如何保证最终一致性

48 消息队列幂等性如何设计

49 Spring常用注解有哪些

50 @Autowired与@Resource区别

51 Spring Boot与Spring区别

52 事务注解@Transactional使用方式

53 @Transactional在什么场景会失效

54 算法题：合并两个有序链表

## 1.3

### （一）自我介绍与 HR 问题（5min）

进行 5 分钟自我介绍，同时被问到实习学到了什么软技能 。

### （二）八股问答（25min）

1. equals 和 hashcode 方法相关问题
    
2. 延伸到 hashmap 的一些常见问题
    
3. java 线程状态
    
4. 线程池，其中问了线程在不用的时候处在什么状态，回答得不太好
    
5. synchronized 和 reentrantlock 锁机制对比，哪种效率高
    
6. mysql 事务隔离级别，为什么要设计四种隔离级别，有点忘记，答得不太好
    
7. redis 的 zset 为什么要用跳表而不是红黑树来实现，没太看过，答得一般
    

### （三）小厂实习相关（10min）

询问小厂实习情况，涉及元空间 oom 的一些问题

### （四）手撕代码（10min）

手撕题目：反转指定位置的链表

### （五）反问环节（5min）

有 5 分钟时间进行反问 。

  

# 字节 9.2

1. 讲一下Java的内存管理？为什么复制算法只用在新生代的回收，不用在老年代？
    
2. 不同线程并发方法的区别（回答了Synchronize和reentrantlock的区别）？问了AQS了解吗？公平锁和非公平锁是怎么实现的
    
3. Redis缓存穿透、击穿、雪崩
    
4. Redis分布式锁，使用过程中可能存在的问题（不清楚问的是redis还是redisson），怎么设置合理的ex时长，锁续期机制是什么实现的？
    
5. TCP拥塞控制机制？（慢启动、拥塞避免、快重传和快恢复）追问还有什么？（真的不清楚）
    
6. 设计模式什么看的比较多？（pity 多看看）
    
7. 场景题：由于c++没有自动内存回收机制，应该怎么表示虚拟内存块，以及采用什么样的数据结构（大概是这个意思）
    
8. 场景题：在几亿用户的场景下，怎么实时更新统计用户的积分，并且展示top100的排行榜？给出积分统计的完整设计方案以及top100排行榜怎么实现
    
9. 手撕：链表倒数第k个节点
    

  

# 美团9.4

1. DDD怎么用的？怎么进行划分的？
    
2. 如果要发优惠券怎么保证一定能发送给用户？（ **“消息队列异步保障 + 接口重试幂等 + 全流程状态追踪 + 定时补偿 + 多渠道兜底”** ）
    
3. 规则树怎么用的？动态是怎么体现的？如果要叠加怎么实现？
    
4. b+树和b树？覆盖索引
    
5. java锁？threadlocal用过吗？强引用会导致什么（内存泄漏）
    
6. 线程池参数
    
7. 给了一道题，问线程池参数怎么修改
    
8. [152. 乘积最大子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-product-subarray/description/?envType=study-plan-v2&envId=top-100-liked)
    

# 得物9.6

聊项目：遇到什么困难？收获是什么？ 八股 1、介绍一下mysql的事务隔离级别 2、介绍一些什么是MVCC 3、read view里面有什么内容 4、MVCC能完全解决幻读吗？ 那怎么解决？ 5、用过哪些中间件？

6、redis为什么适用于高并发场景

7、MQ具体使用？在此处使用的必要性 口撕算法：数组中第k小的元素？回答了堆排序和快排两种思路 追问：时间复杂度？这个时间复杂度是怎么算出来的？ 追问：加个条件，n非常的大，不能一次性把数据加载到内存中，这种情况下怎么实现这种算法？

# 招银网络科技9.8

1. 库存转移的表结构设计？根据仓库编码查询的时候是否会存在索引失效的场景？（字段中有**大量重复数据，会不走索引**）
    
2. 数据一致性是怎么保证的？事务以及分布式锁
    
3. 分布式锁是怎么用的？锁续期机制？
    
4. 运用观察者模式实现库存变更事件驱动架构？怎么实现的？怎么保证数据不丢失以及避免重复消费的问题？
    
5. 手撕：java通过定长数组和信号量实现生产者消费者模型
    
6. 手撕：sql
    

![](https://my.feishu.cn/space/api/box/stream/download/asynccode/?code=YTQ3ZTVmZThmZDZhN2IzMGEyZjUyYWYyNjEyZmFiNDRfNDVsQVdIY3ZpQ3BzWGZ2eUd6UVdmU25ZcW44YjZNcE1fVG9rZW46VE1qT2I1U1lwb0k4ZHV4ZldvbmNBUTFrbmdlXzE3NzM2NTA0NTQ6MTc3MzY1NDA1NF9WNA)

暂时无法在飞书文档外展示此内容

# 招银二面准备

## 1.1

在招行手机银行，你觉得大模型可以在哪些场景上去做一些应用？ 从你所说的一个场景去讲一下怎么选型 作为一个银行开发人员应该具备哪些能力 讲一下你对安全防护的理解 讲一下常用的web攻击手段以及对应的防御措施 拿你所说的SQL注入攻击这块来说，你觉得在开发工程中，包括需求分析、详细设计、开发测试、项目投产，你应该怎么加强去避免这个漏洞呢 一些客户敏感信息可以通过什么工具避免泄漏 秒杀活动怎么做性能优化 一些热点数据在缓存方面怎么更好地实现高可用？ 你自己平时会怎么进行技术方面的自我提升

- 招行手机银行大模型的应用场景及选型
    
    - 应用场景：在招行手机银行，大模型可以应用于智能客服场景，如让 “小招” 从 “预设服务式” 的财富助理，逐步向 “能听会说” 的银行助理进化，在业务办理、疑难咨询等场景提供个性化的一站式解决方案；也可应用于风险管理场景，基于大模型技术快速生成客户风险洞察报告和行业分析 AI 报告，辅助客户经理和信贷人员提升客户尽调和信贷审查效率；还可以应用于经营管理场景，如将资本管理系统与大模型融合，助力资本新规全面落地。
        
    - 选型：对于智能客服场景，选型时要考虑模型的自然语言处理能力，包括语言理解、生成和对话管理等方面的性能。可以选择一些在金融领域有较好应用案例、对金融术语理解准确的大模型。同时，要考虑模型的可扩展性和实时性，以应对大量用户的咨询请求。此外，还需要考虑模型的安全性和隐私保护能力，确保用户信息的安全。
        
- 银行开发人员应具备的能力
    
    - 技术能力：精通 Java、Python 等编程语言，熟练掌握 Spring Boot、微服务架构，熟悉分布式系统设计原理。熟练使用 MySQL、Oracle 等数据库，掌握 Redis、Kafka 等中间件技术。具备大数据开发经验者优先，如熟悉 Hadoop、Spark、Flink 等技术。
        
    - 业务能力：了解银行的业务流程和金融知识，能够将技术与业务需求相结合，更好地实现业务功能。
        
    - 安全能力：具备安全开发的意识和技能，了解常见的安全漏洞和攻击手段，能够采取有效的防护措施，如防止 SQL 注入、数据泄露等。
        
    - 沟通协作能力：能够与团队成员、业务部门进行有效的沟通协作，共同推进项目的进展。
        
- 对安全防护的理解
    
    - 安全防护是保障系统和数据安全的一系列措施和手段。它包括网络安全、应用安全、数据安全等多个方面。网络安全主要是防止网络攻击，如 DDoS 攻击、网络监听等；应用安全则侧重于保护应用程序免受漏洞攻击，如 SQL 注入、跨站脚本攻击等；数据安全主要是确保数据的保密性、完整性和可用性，防止数据泄露、篡改和丢失。安全防护需要从技术、管理和人员等多个层面入手，采取综合的防护策略。
        
- 常用的 web 攻击手段以及对应的防御措施
    
    - SQL 注入攻击：攻击者通过在用户输入字段中插入恶意的 SQL 语句，来获取或篡改数据库中的数据。防御措施包括使用预编译语句、输入验证与过滤、最小化数据库权限、隐藏错误信息等。
        
    - 跨站脚本攻击（XSS）：攻击者在网页中插入恶意的脚本代码，当用户访问该网页时，脚本代码会在用户的浏览器中执行，从而窃取用户的信息。防御措施包括对用户输入进行编码处理、设置 HTTP-only 属性、避免使用 eval 等危险函数等。
        
    - 跨站请求伪造（CSRF）：攻击者伪造用户的请求，让用户在不知情的情况下执行一些非法操作。防御措施包括使用 CSRF 令牌、验证请求来源等。
        
- 在开发工程中避免 SQL 注入漏洞的方法
    
    - 需求分析：明确系统对数据输入的要求和限制，考虑到可能存在的安全风险，将安全需求纳入需求文档。
        
    - 详细设计：设计安全的数据库访问架构，确定使用预编译语句等安全的数据库操作方式，并在设计文档中明确说明。
        
    - 开发测试：开发人员严格按照设计要求使用预编译语句进行数据库操作，对所有用户输入进行严格的验证和过滤。测试人员通过构造恶意输入进行安全测试，检查是否存在 SQL 注入漏洞。
        
    - 项目投产：在项目投产前，进行全面的安全审计和漏洞扫描，确保系统没有 SQL 注入漏洞。同时，部署 Web 应用防火墙（WAF）等安全设备，对生产环境中的流量进行实时监测和防护。
        
- 避免客户敏感信息泄漏的工具
    
    - 数据加密工具：如 AES 加密算法，可以对客户的敏感信息进行加密处理，确保在存储和传输过程中即使信息被窃取，也无法被解密读取。
        
    - Web 应用防火墙（WAF）：可以检测和拦截针对 Web 应用的攻击，防止攻击者通过漏洞获取客户敏感信息。
        
    - 数据脱敏工具：在数据展示和共享时，使用数据脱敏工具对敏感信息进行脱敏处理，如将身份证号码、银行卡号等信息部分隐藏。
        
- 秒杀活动的性能优化
    
    - 缓存优化：使用分布式缓存如 Redis 集群来缓存商品信息、库存等数据，减轻数据库的压力。可以设置多级缓存，如本地缓存和分布式缓存相结合，提高缓存的命中率。
        
    - 数据库优化：对数据库进行优化，如建立合适的索引，优化 SQL 语句，减少数据库的查询时间。
        
    - 限流与削峰：采用限流措施，如限制用户的请求频率，防止过多的请求同时到达服务器，造成服务器瘫痪。可以使用令牌桶算法、漏桶算法等实现限流。
        
    - 异步处理：将一些非关键的业务逻辑，如订单处理后的通知等，采用异步处理的方式，提高系统的响应速度。
        
- 热点数据缓存高可用的实现
    
    - 使用分布式缓存集群：如 Redis 集群，通过主从架构实现高可用，主节点提供读写服务，从节点作为备份。
        
    - 本地缓存辅助：使用本地缓存来解决热 key 问题，将热点数据复制多份到本地缓存，避免所有请求都打到同一个 Redis 实例上。
        
    - 缓存数据同步：通过 Redis 的发布订阅功能等方式，实现缓存数据的同步更新，确保本地缓存和分布式缓存的数据一致性。
        
    - 监控与报警：对缓存的性能和状态进行实时监控，如缓存命中率、内存使用情况等，当出现异常时及时报警，以便及时处理。
        
    
    ##   手撕
    
    - Sql
        
    - 输入流
        

## 9.10 面试

全程项目拷打无八股

# 小红书一面 9.11

1. 请介绍一下你在美的集团实习时，项目难点。
    
2. 你提到在库存属性转移中使用了策略模式，能详细讲讲策略模式在这个场景中的具体应用吗？
    
3. 谈谈你对 HashMap 底层原理的理解，包括 JDK 1.7 和 1.8 的区别。
    
4. 介绍一下 ConcurrentHashMap 在不同 JDK 版本中的锁机制及变化。
    
5. 说说红黑树的特点以及它在 HashMap 中的作用。
    
6. 讲讲你对线程锁的了解，比如 synchronized 和 ReentrantLock 的区别与使用场景。
    
7. 描述一下 JVM 的内存结构，包括各个区域的作用。
    
8. 介绍一下垃圾回收相关的算法，比如引用计数法、可达性算法、三色标记法以及常见的垃圾回收算法（复制算法、标记 - 清除算法、标记 - 整理算法）。
    
9. 你了解哪些垃圾回收器，它们各自的特点是什么？
    
10. 讲讲你对线程池的理解，以及线程池的七大参数。
    
11. 如果要设计一个个人博客系统，你会如何设计表结构？
    
12. 如何实现二叉树的层序遍历？（手撕）
    
13. 你最大的优点和缺点分别是什么？
    
14. 反问：我们面试一共有几个环节？
    
15. 反问：我们部门主要是做什么业务的？
    

# 字节二面 9.12

1. 请介绍一下数据库中的日志类型，比如 redo log、undo log 和二进制日志（binlog），并说明它们的作用和所属层面。
    
2. 你在美的集团参与的制造供应链平台项目主要负责哪些业务？
    
3. 详细描述一下库存属性转移业务中遇到的问题以及解决过程。
    
4. 为什么在库存属性转移业务中选择使用策略模式？策略模式在该场景中的具体应用是怎样的？
    
5. 如果要设计一个类似朋友圈的消息推送系统，你会如何设计数据存储和缓存策略？
    
6. 如何解决缓存与数据库之间的数据一致性问题？
    
7. 比较一下 Redis 和 MySQL 的数据存储特点和适用场景。
    
8. 请解释一下延迟双删策略，并说明它在解决缓存与数据库一致性问题中的作用。
    
9. 算法时间复杂度 组合总和
    

# 得物二面 9.13

1. 请介绍一下你本科和研究生阶段的学业成绩排名情况？
    
2. 你在美的集团实习期间，主要负责了哪些工作内容？在开发过程中遇到过哪些问题，又是如何解决的？
    
3. 在库存属性转移业务中，批次号生成遇到了什么问题？最开始的解决方案是什么，为什么后来调整了方案？你认为调整后的方案优势在哪里？
    
4. 库存转移和库存属性转移业务中，技术上的主要难点是什么？你是如何解决这些难点的？
    
5. 你是如何学习策略模式并将其应用到项目中的？
    
6. 请谈谈 equals 方法和 == 的区别，以及重写 equals 方法时为什么要重写 hashCode 方法？
    
7. volatile 关键字有什么作用？
    
8. 你在项目中使用过 MySQL 和 Redis 吗？使用场景多吗？
    
9. MySQL 中联合索引的查询需要遵循什么原则？如果不遵循该原则，索引会生效吗？
    
10. 请介绍一下 B+ 树的结构和优点，它和 B 树有什么区别？
    
11. MySQL 有哪些事务隔离级别？默认的隔离级别是什么？该默认隔离级别存在什么问题，如何解决？
    
12. 你能举例说明一下 MySQL 中幻读的现象吗？
    
13. 在项目中如果遇到应用占用资源过高的情况，你会如何排查？
    
14. 如何用两个栈实现一个队列？请描述具体的实现思路。
    
15. 你在周末和假期有什么兴趣爱好？除了工作和实习，平时会学习哪些技术或做哪些项目？
    
16. 你之前提到的高并发项目是公益项目吗？之前 HR 说你通过了某机会，目前进展如何？为什么不考虑去佛山那边发展？
    
17. 目前面试的是哪个部门？这个部门是已经确定了吗？
    
18. 本次技术面试一共有几轮？
    

# 虾皮一面 9.13

### 一、项目与业务场景相关问题

1. 你在美的集团实习参与的制造供应链平台中，库存转移的并发安全是如何保证的？库存属性转移为什么选择用策略模式？具体是怎么实现的？
    
2. 你独立实现的高并发拼团平台中，为什么用 Redis 无锁化设计实现库存抢占？相比直接操作数据库有什么优势？还有哪些其他优化手段？
    
3. 高并发项目中异步查询商品信息时，线程池是如何配置和使用的？为什么选择这样的配置（如核心线程数、拒绝策略）？
    
4. 你提到库存属性转移有 4 种类型（订单转订单、订单转共享、共享转订单、物料转物料），除了策略模式，还有其他方案能处理这些复杂类型和状态流转吗？策略模式在其中如何避免大量 if-else 判断？
    

### 二、Java 基础与并发编程问题

1. equals 方法和 == 的区别是什么？重写 equals 方法时为什么必须重写 hashCode 方法？
    
2. volatile 关键字的作用有哪些？它如何保证变量可见性和防止指令重排？
    
3. JDK 1.7 和 JDK 1.8 中 HashMap 的实现有哪些核心区别？（如红黑树引入、扩容机制、哈希冲突解决）
    
4. HashMap 的加载因子为什么默认是 0.75？扩容时为什么是原来的 2 倍？扩容过程中数据如何迁移？
    
5. ConcurrentHashMap 在 JDK 1.7 和 JDK 1.8 中的锁机制有什么不同？1.8 是如何实现高并发安全的？
    
6. Java 中的锁有哪些类型（如偏向锁、轻量级锁、重量级锁）？它们的区别和适用场景是什么？
    
7. ThreadPoolExecutor 的核心参数有哪些？请描述它的任务执行流程（核心线程、等待队列、最大线程、拒绝策略的触发顺序）。
    
8. Java 中的异常分为哪几类？运行时异常和非运行时异常的区别是什么？Error 和 Exception 的区别呢？
    

### 三、JVM 相关问题

1. JVM 的内存区域分为哪几部分？各区域的作用是什么？（如虚拟机栈、程序计数器、堆、方法区等）
    
2. Java 堆内存是如何划分的（新生代、老年代、Survivor 区）？对象的创建和回收流程是怎样的（如从 Eden 区到老年代的转移条件）？
    
3. 新生代和老年代分别采用什么垃圾回收算法？为什么选择这些算法？
    
4. CMS 垃圾回收器的执行流程分为哪几个阶段？哪些阶段会触发 Stop The World（STW）？
    
5. G1 垃圾回收器的核心目标是什么？它和 CMS 相比有哪些优势？
    
6. 如何通过 JVM 参数配置堆内存大小、选择垃圾回收器？你在项目中用过哪些 JVM 参数？
    

### 四、数据库（MySQL）相关问题

1. MySQL 中的日志有哪些类型（如 redo log、undo log、binlog）？它们的所属层面（引擎层 / Server 层）和作用分别是什么？
    
2. InnoDB 为什么选择 B+ 树作为索引结构？B+ 树和 B 树、普通二叉树的区别是什么？
    
3. InnoDB 中的聚簇索引和非聚簇索引有什么区别？非聚簇索引为什么需要 “回表”？如何避免回表？
    
4. 联合索引的 “最左前缀原则” 是什么？如果查询条件不满足最左前缀，索引会失效吗？举个例子说明。
    
5. 哪些情况会导致索引失效？（如函数操作、隐式类型转换、LIKE 以通配符开头、范围查询后的字段）
    
6. 什么是索引下推？它在什么场景下会生效？能解决什么问题？
    
7. 如何排查 MySQL 慢查询？explain 命令的核心输出字段有哪些？如何通过 explain 判断索引是否被使用？
    
8. 数据库死锁怎么排查
    

### 五、Redis 与分布式相关问题

1. 如何用 Redis 实现分布式锁？需要注意哪些问题（如锁超时、重入性、释放别人的锁）？Redis 的看门狗（Watch Dog）机制是如何实现锁续期的？
    
2. Redis 中的缓存穿透是什么？有哪些解决方案（如布隆过滤器、空值缓存）？布隆过滤器的原理是什么？
    
3. 缓存与数据库的数据一致性如何保证？（如更新数据库后是否删缓存、延迟双删的作用）
    

### 六、网络与协议相关问题

1. TCP 三次握手的流程是什么？为什么需要三次握手而不是两次？
    
2. TCP 四次挥手的流程是什么？为什么需要四次挥手？TIME-WAIT 状态的作用是什么？
    
3. TCP 的拥塞控制机制是怎样的？（如慢启动、拥塞避免、快速重传、快速恢复）
    
4. OSI 七层模型分为哪几层？各层的核心协议有哪些？（如传输层的 TCP/UDP、网络层的 IP）
    

### 七、AOP 与设计模式相关问题

1. AOP（面向切面编程）的核心概念是什么？（如切面、通知、切入点、代理对象）它是如何实现的（如动态代理）？
    
2. 你在项目中用 AOP 做过什么场景？（如权限校验）具体是如何实现的？
    
3. 抽象类和接口的区别是什么？什么时候该用抽象类，什么时候该用接口？
    
4. 除了策略模式，你还了解哪些设计模式？在项目中用过哪些（如工厂模式、单例模式）？
    

### 八、面试流程与职业相关问题

1. 我们部门主要负责哪些业务？你对未来负责的业务方向有什么期待？
    
2. 结合你今天的面试表现，你觉得自己有哪些需要提升的地方？希望我给你提些什么改进意见？
    

# 京东一面 9.15

1. 请做一下自我介绍，包括你的教育背景、研究方向、实习经历以及掌握的技术能力。
    
2. 你在过往项目（或实习）中遇到过哪些比较难解决的技术难点？请具体说说这个难点是什么，你最开始的解决方案、遇到的问题，以及最终如何解决的。
    
3. 从这些项目（或实习）经历中，你觉得自己最大的收获是什么？
    
4. 如何寻找两个有序数组的中位数？请说说你的思路。
    
5. 如何实现螺旋矩阵的遍历？请讲讲具体的实现逻辑（或指针操作思路）。
    
6. 什么是指令重排？指令重排可能会导致什么问题？如何避免指令重排带来的影响？
    
7. volatile 关键字有哪些作用？请具体说明。
    
8. 操作系统中，进程调度有哪些常见的算法？
    
9. Spring 框架的核心原理是什么？请分别说说 IOC 和 AOP 的概念及作用。
    
10. 你如何评价自己的学习能力？请结合具体经历说明。
    

# 科大讯飞

1. JVM垃圾回收器
    
2. 分布式锁（如 Redis 分布式锁）的实现原理是什么？
    
3. Redis 的分布式锁如何保证加锁和设置过期时间的原子性？
    
4. 常见的垃圾回收器有哪些？
    
5. 服务宕机后如何排查问题？
    
6. 如何防止消息重复消费？
    
7. 如何保证消息的有序性？
    
8. 死信队列的作用是什么？
    
9. Redis 有哪些数据结构？各有什么应用场景？
    
10. Redis 的持久化方式有哪些？各自的原理和特点是什么？
    
11. 持久化过程中有新的操作进来会怎样处理？
    
12. Redis 的主从复制和哨兵模式是怎样的？
    
13. 数据库性能优化有哪些方法（如分库分表等）？
    
14. 如何应对任务延期的情况，保证任务按期推进？
    
15. 最近学习了哪些新知识？
    
16. 对大模型（如 RAG 等）的理解是怎样的？
    
17. 对面试部门主要负责的业务有什么想了解的？
    
18. mysql除了优化语句，还能怎么进行优化
    
19. redisson使用和实现
    
20. Rebbitmq 防止重复消费的方法
    
21. redis主从复制
    

# 携程9.22

1. 你认为在数据库索引优化方面，有哪些关键思路或注意事项？
    
2. 在业务中，如何实现业务锁？你们项目中具体采用了什么方案，为什么选择该方案？
    
3. 在使用 Redis（推测 “radiation”“release” 为口误）实现业务锁时，key 和 value 通常会如何设计？比如限制某个按钮操作的场景下，key 的标识逻辑是什么？
    
4. Redis 支持哪些主要的数据类型？在实际业务中，不同数据类型（如 string、hash、zset 等）分别适用于哪些场景？比如对象数据（如订单信息）更适合用哪种类型存储，为什么？
    
5. 平时会使用哪些工具进行接口调试和单元测试？在高并发平台项目的单元测试中，具体是如何操作的（比如如何构造参数、验证结果）？
    
6. 当需要从一亿条数据中找出最大值时，你会采用什么方案？分块处理的逻辑是什么，是否需要分块的判断标准是什么？
    
7. 你的职业规划是怎样的？比如短期和长期在技术成长、职位发展上有什么目标？
    

# 字节三面9.22

1. 你在美的集团物流部门实习时，所在团队负责的供应链平台具体有哪些核心功能？
    
2. 你在实习中负责的 “库存转移” 和 “库存属性转移” 这两个需求，具体业务场景是怎样的？
    
3. 在库存属性转移需求中，你遇到的 “批次号生成” 挑战具体是什么？最开始的解决方案存在什么问题，最终是如何解决的？
    
4. 你提到批次号需要保证唯一性和出库顺序（与入库顺序一致），如果用雪花算法这类生成唯一 ID 的方式为何不符合场景？如果单独加一个字段来表示顺序，在库存拆分（如 100 个拆成 90 和 10）的场景下，该字段的顺序该如何定义？
    
5. 你在项目中使用过哪些设计模式？比如策略模式、观察者模式具体是如何应用的？还了解哪些其他设计模式？
    
6. 你认为在项目中使用设计模式有什么优势？
    
7. 你了解乐观锁和悲观锁吗？它们的核心区别是什么？分别适用于什么场景？
    
8. 你本科和研究生期间的学业成绩如何（排名 / 绩点情况）？
    
9. 你提到的 “练手项目” 是实习相关的，还是个人独立完成的？
    
10. 常用的 Java 集合有哪些？比如 ArrayList 和 LinkedList 的核心区别是什么（从数据结构、访问 / 增删效率角度）？
    
11. 了解 ConcurrentHashMap 吗？它在 JDK1.7 和 JDK1.8 版本中的实现有什么区别（比如锁机制、数据结构）？
    
12. 在高并发场景（如大促）下，如何保障系统稳定？比如在缓存、限流、削峰、熔断等方面有哪些思路？
    
13. 什么是熔断机制？它的核心逻辑是什么（比如熔断的开启、半开、关闭状态如何切换）？
    
14. 常见的服务限流算法有哪些？
    
15. 目前你拿到了哪些 offer？对于美的和美团（你提到约了 HR 面），你的考虑是什么？
    
16. 你的职业规划是怎样的？短期和长期在技术成长、职位发展上有什么目标？
    
17. 未来工作地点方面，你更倾向于哪些城市？不考虑佛山的原因是什么？
    
18. 我们部门负责的业务你可能记不太清了，想再确认一下，你想了解我们部门具体负责什么业务吗？
    
19. 想了解一下公司对新人有哪些培养计划？
    
20. 我们的面试大概有几轮？目前是否还有后续面试环节？
    
21. 你明年的毕业时间是什么时候？如果拿到 offer，后续是否有时间参与实习（比如受毕业论文进度影响）？
    
22. 除了以上问题，你还有其他想了解的内容吗？
    

# 美团一面

1. 请做一下自我介绍，包括教育背景、实习 / 项目经历、技术能力及所获荣誉。
    
2. 你独立实现的高并发拼团平台，是参考什么做的？属于个人练手项目还是其他类型？
    
3. 这个拼团平台的整体架构（比如你提到的 DDD 架构）和核心业务逻辑（尤其是交易域的营销优惠策略实现）是怎样的？
    
4. 拼团平台支持哪些营销优惠类型？这些优惠是如何根据用户标签判断是否生效，以及是否支持叠加？叠加规则是什么？
    
5. 拼团场景中，为什么要采用 Redis 实现库存抢占？具体是如何通过 Redis 操作减轻数据库行锁压力的？
    
6. 拼团平台的 “试算” 和 “结算” 在业务流程和数据处理上有什么区别？
    
7. 你了解消息队列（比如你提到的类似 RocketMQ/Kafka 的机制）如何保证消息不丢失吗？具体有哪些保障机制？
    
8. 你的 Java 技术是如何学习的？是学校课程还是自学？
    
9. Java 中 “==” 和 equals () 的区别是什么？为什么重写 equals () 时通常要重写 hashCode ()？
    
10. String、StringBuilder 和 StringBuffer 的区别是什么？分别适用于什么场景？
    
11. 线程池的核心参数有哪些？默认拒绝策略是什么？线程池的任务执行流程（核心线程、等待队列、最大线程的触发逻辑）是怎样的？线程池中的 “存活时间” 参数有什么作用？
    
12. 你熟悉哪些数据库和缓存技术？（比如是否熟悉 MySQL 和 Redis）
    
13. MySQL 索引的默认存储结构是什么（比如 B + 树）？B + 树和 B 树的区别是什么？B + 树在查询效率和范围查询上有什么优势？
    
14. 什么是聚集索引和非聚集索引？两者在数据存储和查询上有什么区别？什么是覆盖索引？覆盖索引能解决什么问题？
    
15. MySQL 联合索引的 “最左匹配原则” 是什么？哪些情况会导致联合索引失效？
    
16. 如何判断 SQL 语句是否使用了索引？可以通过什么工具或命令查看 SQL 执行计划，主要关注哪些字段？
    
17. 数据库事务的 ACID 属性分别指什么？各属性是如何保证的（比如原子性、持久性的实现依赖）？
    
18. 什么是 MySQL 的 MVCC？它的核心组成部分有哪些？是如何实现事务隔离性和数据可见性判断的？
    
19. 你是否看过 JVM 相关的知识？JVM 的内存区域是如何划分的？各区域的主要作用是什么？
    
20. JVM 常见的垃圾回收算法有哪些？（比如复制算法、标记 - 清除、标记 - 整理）各算法的核心逻辑、优缺点是什么？
    
21. 你在项目中使用观察者模式的具体场景是什么？（比如你提到的库存操作后通知其他服务修改订单状态）
    
22. 你了解工厂模式吗？工厂模式的核心思想和实现方式是怎样的？
    
23. Redis 支持哪些主要的数据类型？你在项目中使用过 Redis 的 bitmap 吗？bitmap 的使用场景是什么（比如用户标签判断）？
    
24. Redis 的用户标签信息是如何录入的？是手动录入还是自动同步？
    
25. Redis 有什么优势？为什么它的处理速度比较快？
    
26. 在缓存和数据库双写场景下，如何避免数据不一致问题？你通常采用什么方案？
    
27. 什么是缓存穿透、缓存击穿、缓存雪崩？分别有哪些解决方案？
    
28. 如何从海量数据中找到第 K 大的数？请讲讲你的实现思路（比如你提到的快排思想）。在快排思想实现过程中，如何选择枢纽才能保证较好的时间复杂度？
    
29. 你想了解我们部门主要负责什么业务吗？
    
30. 除了以上问题，你还有其他想了解的内容吗？
    

# 字节五面

1. 请先做一下自我介绍，包括教育背景、实习经历、项目经验、技术能力及所获荣誉。
    
2. 你在美的集团安德物流部门实习时，参与开发的制造供应链平台具体有哪些功能？你主要负责哪些模块，核心业务逻辑（如库存转移、库存属性转移）是怎样的？
    
3. 在库存属性转移需求中，批次号生成遇到了什么问题？最开始的解决方案存在什么缺陷，最终是如何优化的？
    
4. 这个制造供应链平台的应用场景是什么？主要服务于哪些客户（如美的内部、海信等外部企业）？
    
5. 该平台是自研的还是基于套装软件开发的？安德物流部门在其中承担什么角色？
    
6. 你认为这个平台的核心价值或解决的核心问题是什么？是否有涉及服务性能优化（如响应速度）的工作？
    
7. 在项目中，权限校验是如何实现的？为什么会选择用AOP来做权限管理？AOP的实现原理是什么，它属于哪种设计模式？
    
8. 你在项目中使用策略模式的契机是什么？策略模式解决了什么问题，相比传统的if-else判断有什么优势？
    
9. 你对MySQL的底层原理了解多少？请讲讲MySQL的存储引擎、事务ACID属性的实现机制、MVCC工作原理以及索引相关知识（如索引结构、覆盖索引、索引下推、最左匹配原则）。
    
10. 你本科期间学过哪些计算机相关的核心课程？
    
11. 你在开发过程中会使用AI工具吗？主要用哪些AI工具（如通义灵码、Copilot）？这些工具在哪些场景下帮到你（如生成结构体、理解项目、写复杂方法）？
    
12. 对比你使用过的AI工具（如通义灵码和Copilot），它们在能力上有什么差异？你更倾向于在什么场景下使用它们？
    
13. 请你讲讲如何实现一个LRU缓存的Put操作？核心逻辑是什么（如判断缓存大小、元素的添加与移除、队列与Map的协同操作）？
    
14. 除了以上问题，你还有其他想了解的内容吗？
    

交易 toc 个人用户 tob 京东万商 线下门店 java springboot mysql

设计流程 不同的开发有四季的思考 不统一肯恩恶搞不合理 方案评估

观察者模式

更高层面 架构怎么保证架构 避免重构

  

java基础补一下 开发过程中 是对知识的一个体现

看源码 springbboot源码

练手的项目 要去思考 有没有更好的解决方案 做一些拓展 你觉得应该是怎样的

加个排行榜/热销榜

  

# 京东一面12.23

- 请做一个自我介绍。
    
- 本科期间有实习经历吗？
    
- 在美的实习期间，你做的最核心的业务是什么？（库存转移/属性转移）
    
- **深挖：** 你提到的“批次号生成”遇到了什么问题？具体是怎么解决的？
    
- 这个模块团队有几个人？是你独立负责的吗？
    
- 在这个业务中用到了 MQ 吗？还是只用了观察者模式？
    
- **场景题：** 库存修改和发消息通知订单更新，是在同一个事务里吗？如何保证一致性？（如果发消息失败了怎么办？）
    
- 为什么要使用策略模式？
    
- 你在项目中用到了 AOP 做权限校验，具体是怎么通过上下文 (Context) 获取用户信息的？
    
- Spring Boot 相比于 Spring 有什么优势？原理是什么？
    
- 你有看过 Spring Boot 的底层源码吗？
    
- MySQL 的索引底层结构是什么？为什么用 B+ 树？
    
- 聚簇索引和非聚簇索引的区别？
    
- MySQL 的 ACID 特性分别靠什么保证？
    
- Redis 用过哪些数据结构？分布式锁是怎么实现的？（SetNX / Redisson）
    
- **设计题：** 观察者模式（内存中）和 MQ（中间件）有什么区别？为什么说 MQ 更可靠？
    

# 字节一面3.11

**业务与项目经历**

1. 请进行自我介绍。
    
2. 在美的实习的具体时长是多少？
    
3. 简历中的练手项目是自驱完成的还是实习期间的产出？
    
4. 比赛项目（物料匹配）的具体赛题是什么？团队的分工如何？
    
5. 为什么选择去美的？
    
6. 你所在的“制造执行计划”模块在整个供应链平台中的业务定位是什么？
    
7. 该模块的上下游依赖关系是什么？
    
8. 库存转移解决的实际物理场景是什么？（入库区、普通仓储区、出库区的界定及流转逻辑）
    
9. 订单入库后，在什么触发条件下会转移到普通仓储区？
    
10. 针对需求文档，你的系统设计与表修改逻辑是怎样的？（库存表、托盘绑定表）
    
11. 托盘和货位在数据库层面是如何建立绑定关系的？
    
12. 批次号冲突的根本原因是什么？（Redis 自增逻辑与数据库历史订单读取逻辑的冲突）
    
13. 实习期间遇到的最大技术挑战是什么？
    
14. 为什么实习不到三个月，功能尚未完成测试上线就离职？
    

**架构设计与框架**

15. 策略模式在代码层面是如何设计与抽象的？（出入口定义）
    
16. 不同库存属性转移类型在底层业务逻辑上的本质差异是什么？
    
17. 主方法中如何动态获取对应的策略实现类？
    
18. Spring 框架下，策略类是如何通过注解实现自动注册和自我管理的？底层机制是什么？
    

**数据库与高并发**

19. 实习项目中遇到过哪些并发问题？并发量级如何？
    
20. 数据库索引优化的具体手段有哪些？（覆盖索引、最左前缀原则失效场景）
    
21. 针对大数据量出勤信息的展示，Limit/Offset 深分页查询应如何优化？
    

**Java 底层与 JVM**

22. 个人项目使用的是哪个 JDK 版本？JDK 17 有哪些新特性？
    
23. 简述 JVM 的内存模型及各分区职责。
    
24. JDK 8 的默认垃圾回收器是什么？常用的垃圾回收器有哪些？
    
25. G1 和 CMS 的核心区别与特性是什么？
    
26. G1 的具体回收流程是怎样的？哪些阶段会触发 STW (Stop The World)？STW 的线程挂起机制？
    
27. HashMap 在 JDK 1.7 到 1.8 发生了哪些底层数据结构的变化？为什么引入红黑树？
    
28. HashMap 为什么是线程不安全的？举例说明其引发的数据不一致场景。
    
29. 多线程下应如何选择安全的集合容器？Hashtable 为什么效率低下？
    
30. ThreadLocal 的底层存储结构是什么？
    
31. ThreadLocal 为什么会导致内存泄露？其内部 Key 和 Value 的引用类型分别是什么？
    
32. 在项目 AOP 鉴权链路中，具体是如何使用 ThreadLocal 传递用户信息的？
    

**中间件与前沿技术**

33. 消息队列（RocketMQ）是如何保证全链路消息不丢失的？（ACK 机制）
    
34. 消息中间件本身的底层持久化存储机制是怎样的？
    
35. 常见几种MQ的区别？rabbitmq的优点
    
36. 了解过哪些大模型应用层面的技术规范？
    
37. 解释 MCP 协议、AI Agent 以及 RAG（检索增强生成）的工作原理。
    
38. 向量数据库的底层检索算法和机制是什么？
    

**手撕代码**

38. $$O(1$$ 空间复杂度下的算法实现：寻找重复数。
    

  

# 京东一面3.13

1. **数据库与存储底层 (MySQL / Redis)**
    

- 业务表设计时，如何考虑索引优化与字段原子性？
    
- 出勤表仅存员工 ID 并通过 JOIN 联表查询员工信息，在扩展性上会面临什么致命问题？
    
- Hash 索引与 B+ 树索引的底层结构差异与优缺点对比？
    
- MVCC（多版本并发控制）中，读已提交（RC）和可重复读（RR）的底层 Read View 生成机制有何不同？
    
- MVCC 的具体实现原理是什么？（隐藏字段、Undo Log 链）
    
- mybatis分页插件https://www.yuque.com/hollis666/gg1x9v/ogng83zwfrqblu5e
    
- 美的的项目监控部分是怎么做的？（完全没接触过不清楚）
    

2. **分布式系统与架构设计**
    

- 一致性哈希（Consistent Hashing）解决了普通哈希的什么物理痛点？
    
- Redisson 分布式锁底层是如何通过 Lua 脚本保证释放锁的原子性的？
    
- Watchdog 锁续期机制的底层工作流程与触发条件？如果要你自己实现怎么做？（跟队列相关）
    
- 为什么在库存属性转移模块选用策略模式？
    

3. **核心并发场景追问（导致你崩盘的高危考点）**
    

- **（核心拷问）拼团项目中，Redis 抢占库存失败后，你的回滚机制**是如何设计的？
    
- 在高并发场景下，如何使用 `CompletableFuture` 进行数据的异步预加载与编排？
    
- ReentrantLock 的底层实现原理是什么？（AQS、CAS、双向队列）
    

4. **框架、中间件与网络基础**
    

- RabbitMQ 的消息投递语义有哪些？（最多一次、至少一次、精确一次）
    
- HTTP get post报文格式差异？1.0的底层协议
    
- 滑动窗口算法
    
- Spring 框架的动态代理机制原理？默认代理与 CGLIB 的区别是什么？
    
- @service bean是怎么注册的
    

5. **前沿技术（AI 方向）**
    

- 阐述 RAG（检索增强生成）的工作流原理与向量库检索机制。
    
- mcp协议具体有了解吗
    
- mcp和tools的区别
    
- 多agent相关
    
- 怎样让一个不思考大模型，思考之后再回答